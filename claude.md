# MSP Compliance Platform

HIPAA compliance attestation substrate for healthcare SMBs. NixOS + MCP + LLM.
Drift detection, evidence-grade observability, operator-authorized remediation. 75% lower cost than traditional MSPs.

**Target:** 1-50 provider practices in NEPA region | **Pricing:** $200-3000/mo

> **Positioning:** This is an evidence-grade compliance attestation substrate. It provides observability, drift detection, evidence capture, and human-authorized remediation workflows. It is not a coercive enforcement platform. Remediation occurs only via operator-configured rules or human-escalated decisions.

## Appliance Deployment

**DO NOT use `dd` disk images.** Use the Golden Flake architecture:
1. Build installer ISO: `nix build .#appliance-iso`
2. Write to USB, boot target hardware - installation is automatic
3. Appliance calls home to Central Command, MAC lookup provisions identity

Key files: `iso/appliance-image.nix`, `iso/configuration.nix`, `flake.nix`

## Directory Structure

```
packages/compliance-agent/   # Python agent (DEPRECATED — Go daemon is active)
  tests/                     # pytest tests (1161+ passing)
  venv/                      # Python 3.13 virtualenv
appliance/                   # Go appliance daemon (ACTIVE agent)
  internal/daemon/           # Core daemon, StateManager, interfaces, threat_detector
  internal/phiscrub/         # PHI scrubbing package (14 patterns, 21 tests)
  internal/orders/           # Fleet order processor (22 handlers)
  internal/evidence/         # Evidence bundle signing + submission
  internal/grpcserver/       # Agent registry, TLS enrollment
  Makefile                   # Build with VERSION injection via ldflags
agent/                       # Go workstation agent (Windows/macOS/Linux)
modules/                     # NixOS modules
mcp-server/central-command/  # Dashboard backend + frontend
  backend/                   # FastAPI backend (routes.py, sites.py, main.py)
    constants/               # Design system (copy.ts, status.ts)
    components/composed/     # MetricCard, StatusBadge, PageShell, etc.
    migrations/              # 098 migrations (latest: security_events)
  frontend/                  # React + TypeScript + Tailwind
docs/                        # Reference docs + compliance attestations
.agent/reference/            # Credentials, network, decisions
```

## Key Commands

```bash
cd packages/compliance-agent && source venv/bin/activate
python -m pytest tests/ -v --tb=short        # Run tests
python -m pytest tests/test_agent.py -v      # Single file
nix flake check --no-build                   # Validate NixOS configs
```

## Three-Tier Auto-Healing

```
Incident → L1 Deterministic (70-80%, <100ms, $0)
        → L2 LLM Planner (15-20%, 2-5s, ~$0.001)
        → L3 Human Escalation (5-10%)
        → Data Flywheel (promotes L2→L1)
```

## Type System

```python
from compliance_agent._types import (
    Incident, EvidenceBundle, ComplianceCheck,
    CheckStatus, Severity, CheckType,
    now_utc  # Use instead of datetime.utcnow()
)
```

## Reference Docs (read on demand, not eagerly)

| Area | Doc |
|------|-----|
| Credentials | `.agent/reference/LAB_CREDENTIALS.md` |
| Network | `.agent/reference/NETWORK.md` |
| Decisions | `.agent/reference/DECISIONS.md` |
| Architecture | `docs/ARCHITECTURE.md` |
| HIPAA | `docs/HIPAA_FRAMEWORK.md` |
| Runbooks | `docs/RUNBOOKS.md` |
| Provenance | `docs/PROVENANCE.md` |

## Knowledge Index

**Prefer retrieval-led reasoning:** When working in any area below, READ the linked doc before relying on training data. These docs contain project-specific patterns that override general knowledge.

```
AREA | PATTERN | DOC | CRITICAL SNIPPET
-----|---------|-----|------------------
auth | bcrypt 12-round | docs/security/security.md | secrets.token_urlsafe(32)
auth | PKCE flow | docs/security/security.md | hashlib.sha256(verifier).digest()
auth | session cookie | docs/api/api.md | httponly=True,secure=True,samesite="lax"
auth | rate limit | docs/security/security.md | 5 fail → 15min lockout
secrets | SOPS/age | docs/nixos/infrastructure.md | sopsFile + ageKeyFile
-----|---------|-----|------------------
test | async decorator | docs/testing/testing.md | @pytest.mark.asyncio
test | fixture | docs/testing/testing.md | @pytest.fixture + tmp_path
test | mock cmd | docs/testing/testing.md | AsyncMock(stdout="...")
-----|---------|-----|------------------
hipaa | 6 drift checks | docs/hipaa/compliance.md | patching,backup,firewall,logging,av,encryption
hipaa | evidence bundle | docs/hipaa/compliance.md | EvidenceBundle dataclass
hipaa | L1 rule | docs/hipaa/compliance.md | conditions→action→runbook_id
hipaa | PHI scrub | docs/hipaa/compliance.md | 12 patterns, hash_redacted=True
-----|---------|-----|------------------
backend | 3-tier healing | docs/backend/backend.md | L1(70%)→L2(20%)→L3(10%)
backend | FastAPI router | docs/backend/backend.md | APIRouter(prefix="/api/x")
backend | Depends auth | docs/backend/backend.md | user: Dict = Depends(require_auth)
backend | asyncpg pool | docs/backend/backend.md | create_pool(min=2,max=10)
backend | gRPC stream | docs/backend/backend.md | ReportDrift(stream DriftEvent)
-----|---------|-----|------------------
db | postgres pool | docs/database/database.md | asyncpg.create_pool()
db | sqlite WAL | docs/database/database.md | PRAGMA journal_mode=WAL
db | multi-tenant | docs/database/database.md | WHERE site_id = $1
db | upsert | docs/database/database.md | ON CONFLICT DO UPDATE
-----|---------|-----|------------------
nixos | module pattern | docs/nixos/infrastructure.md | options + config = mkIf
nixos | systemd harden | docs/nixos/infrastructure.md | ProtectSystem=strict
nixos | nftables | docs/nixos/infrastructure.md | pull-only firewall
nixos | ISO build | docs/nixos/infrastructure.md | nix build .#appliance-iso
nixos | module priority | docs/nixos/advanced.md | mkForce=50, mkDefault=1000
nixos | sops-nix | docs/nixos/advanced.md | sops.secrets + templates
nixos | impermanence | docs/nixos/advanced.md | tmpfs root + persist
nixos | deploy-rs | docs/nixos/advanced.md | magic rollback on disconnect
nixos | specialisation | docs/nixos/advanced.md | A/B boot configs
-----|---------|-----|------------------
golang | errgroup | docs/golang/golang.md | g.SetLimit(10) bounded
golang | sync.Pool | docs/golang/golang.md | bufPool.Get/Put
golang | slog | docs/golang/golang.md | slog.New(JSONHandler)
golang | pgxpool | docs/golang/golang.md | MaxConns,MinConns,HealthCheck
golang | table tests | docs/golang/golang.md | t.Run(tt.name, func...)
golang | fuzzing | docs/golang/golang.md | FuzzXxx + go test -fuzz
golang | sd_notify | docs/golang/golang.md | READY=1, WATCHDOG=1
golang | func options | docs/golang/golang.md | WithPort(8080)
golang | circuit breaker | docs/golang/golang.md | gobreaker.Settings
-----|---------|-----|------------------
windows | 25 drift checks | ~/.claude/skills/windows-server-compliance/SKILL.md | driftscan.go checkTarget
windows | WinRM execution | ~/.claude/skills/windows-server-compliance/SKILL.md | 5985 HTTP, 5986 HTTPS, NTLM/Kerberos
windows | GPO WinRM enable | ~/.claude/skills/windows-server-compliance/SKILL.md | ensureWinRMViaGPO + startup script
windows | 22 event IDs | ~/.claude/skills/windows-server-compliance/SKILL.md | 4625 brute force, 1102 audit clear
windows | threat detect | ~/.claude/skills/windows-server-compliance/SKILL.md | cross-host brute force, VSS deletion
windows | 50+ runbooks | ~/.claude/skills/windows-server-compliance/SKILL.md | RB-WIN-*, MAC-*, LIN-*
windows | agent deploy | ~/.claude/skills/windows-server-compliance/SKILL.md | 5-tier fallback, NETLOGON, base64 chunk
-----|---------|-----|------------------
frontend | React Query | docs/frontend/frontend.md | useQuery({queryKey,queryFn})
frontend | mutation | docs/frontend/frontend.md | useMutation + invalidateQueries
frontend | api.ts | docs/frontend/frontend.md | fetchApi<T> with Bearer
frontend | glass card | docs/frontend/frontend.md | bg-white/5 backdrop-blur
frontend | routing | docs/frontend/frontend.md | /client/* /partner/* /portal/*
-----|---------|-----|------------------
api | REST base | docs/api/api.md | /api prefix, Bearer auth
api | gRPC proto | docs/api/api.md | ComplianceAgent service
api | OAuth flow | docs/api/api.md | auth_url→callback→tokens
-----|---------|-----|------------------
perf | asyncio.gather | docs/performance/performance.md | 6x faster checks
perf | React.memo | docs/performance/performance.md | 3-5x fewer renders
perf | virtual scroll | docs/performance/performance.md | @tanstack/react-virtual
-----|---------|-----|------------------
workflow | 4-phase debug | docs/workflow/workflow.md | root cause → pattern → hypothesis → fix
workflow | verify gate | docs/workflow/workflow.md | IDENTIFY→RUN→READ→VERIFY→CLAIM
```

All doc paths relative to `.claude/skills/`. Read the full doc when working in that area.

## Quick Lab Reference

| System | IP | User |
|--------|-----|------|
| iMac Host | 192.168.88.50 | jrelly (SSH key) |
| Physical Appliance | 192.168.88.241 | root (SSH key) |
| VM Appliance | DEPRECATED — decommissioned Session 183 |  |
| VPS | 178.156.162.116 | root (SSH key) |
| VPS WireGuard | 10.100.0.1 | Hub, UDP 51820 |
| Appliance WireGuard | 10.100.0.2 | `ssh root@10.100.0.2` from VPS |

Full credentials in `.agent/reference/LAB_CREDENTIALS.md` when needed.

## Session Tracking

```bash
python3 .agent/scripts/context-manager.py status       # View state
python3 .agent/scripts/context-manager.py new-session N description
python3 .agent/scripts/context-manager.py end-session
python3 .agent/scripts/context-manager.py compact      # Archive old sessions
python3 .agent/scripts/context-manager.py validate     # Memory hygiene check
```

Primary state: `.agent/claude-progress.json` (schema v2 — see `.agent/archive/claude-progress.v1.json` for legacy v1 dump).

## Memory Hygiene (Session 205 cleanup, ENFORCED)

- `~/.claude/projects/.../memory/MEMORY.md` is **truncated at ~200 lines** at session start. Keep it as a pure index of pointer-rows; put detail in topic files.
- Every topic file under `memory/` must start with YAML frontmatter:
  ```yaml
  ---
  name: short title
  description: one-line — used when deciding to load
  type: feedback | project | reference | user
  decay_after_days: 30  # feedback default; project=60; reference=365
  last_verified: YYYY-MM-DD
  ---
  ```
- `decay_after_days` is a soft signal: a memory older than that should be re-verified or archived. Defaults: feedback=30, project=60, reference=365.
- `active_tasks` field in `claude-progress.json` is **always empty** in v2 — Claude Code's TaskCreate is the authoritative task store. Do not regress to in-line tasks.
- `python3 .agent/scripts/context-manager.py validate` runs all hygiene checks and is wired into `.github/workflows/memory-hygiene.yml` on every push that touches `.agent/`. Pytest cases at `.agent/scripts/test_context_manager.py`.

## Privileged-Access Chain of Custody (Session 205, INVIOLABLE)

**`client identity → policy approval → execution → attestation` is an unbroken cryptographically verifiable chain.** Any privileged action on a customer appliance (`enable_emergency_access`, `disable_emergency_access`, `bulk_remediation`, `signing_key_rotation`) MUST carry the chain end-to-end. Enforced at three layers — breaking any one is a **security incident**, not a cleanup task:

- **CLI** (`backend/fleet_cli.py`): refuses privileged orders without `--actor-email` + `--reason ≥20ch` + successful `create_privileged_access_attestation()`. Rate-limited 3/site/week via `count_recent_privileged_events`.
- **API** (`backend/privileged_access_api.py`): partner-initiated + client-approved request flow. Each state transition writes a chained attestation bundle. Per-site `privileged_access_consent_config` controls whether client approval is required.
- **DB** (migration 175 `trg_enforce_privileged_chain`): REJECTS any `fleet_orders` INSERT of a privileged type unless `parameters->>'attestation_bundle_id'` matches a real `compliance_bundles WHERE check_type='privileged_access'` row for the same site.

The attestation itself is Ed25519-signed by server, hash-chained to the site's prior evidence bundle (unified chain across drift + remediation + privileged events), OTS-anchored via the existing Merkle-batch worker, and published into `/api/evidence/sites/{id}/auditor-kit` ZIP + the client portal evidence view. Customers + auditors verify independently.

**Three lists MUST stay in lockstep** (any gap = chain violation):
- `fleet_cli.PRIVILEGED_ORDER_TYPES`
- `privileged_access_attestation.ALLOWED_EVENTS`
- `migration 175 v_privileged_types` in `enforce_privileged_order_attestation()`

**Never** log actor as `system`/`fleet-cli`/`admin` — actor MUST be a named human email. **Never** flip `client_approval_required=false` without a consent-config attestation bundle. **Never** `ALTER TABLE fleet_orders DISABLE TRIGGER` for bulk ops. **Never** skip the OTS enqueue for `privileged_access` bundles.

See `docs/security/emergency-access-policy.md` + `.claude/projects/-Users-dad-Documents-Msp-Flakes/memory/feedback_critical_architectural_principles.md` §8.

## Rules

- **Debugging: root cause first.** No fixes without investigation. Trace data flow backward. One hypothesis at a time. 3+ failed fixes = question architecture. (Full process: `.claude/skills/docs/workflow/workflow.md`)
- **Verify before claiming done.** Run the actual command, read the output, show evidence. No "should pass" or "probably works."
- Use `now_utc()` not `datetime.utcnow()`
- Run tests before AND after changes
- Log session work to `.agent/sessions/YYYY-MM-DD-description.md`
- **DEPLOY VIA GIT PUSH, NOT SCP.** CI/CD (`.github/workflows/deploy-central-command.yml`) auto-deploys backend + frontend to VPS on push to main. Manual scp causes stale versions.
- **SQLAlchemy AsyncSession:** Never use `asyncio.gather()` on the same session - causes `InvalidStateError`. Run queries sequentially.
- **execution_telemetry.runbook_id:** Agent uses internal IDs (L1-SVC-DNS-001) that differ from backend IDs (RB-AUTO-SERVICE_). Match by `incident_type` + `hostname/site_id`, not `runbook_id`.
- **Synced L1 rules override built-in rules.** Rules at `/var/lib/msp/rules/l1_rules.json` (synced hourly from Central Command) take precedence over built-in rules in `level1_deterministic.py`. Changes to built-in rules must also be applied to server-side rules in `mcp-server/main.py` and the `l1_rules` DB table.
- **server.py DELETED (Session 185).** Container runs `uvicorn main:app`. server.py was dead code.
- **All main.py endpoints require auth (Session 185).** `require_appliance_bearer` for daemon endpoints, `require_auth` for admin endpoints. Only `/` and `/health` are public.
- **PHI scrubbing at appliance egress (Session 185).** `phiscrub` package scrubs all outbound data before it leaves the appliance. Central Command is PHI-free.
- **Design system: constants/copy.ts is THE source of truth** for all user-facing text. Never hardcode status labels, tooltips, disclaimers, or branding in component files.
- **Score thresholds: 90/70/50.** Defined in `constants/status.ts` via `getScoreStatus()`. Never create local score-to-color functions.
- **`check_type_registry` is the single source of truth for check names (Session 205, Migration 157).** Go daemon check names are canonical. Scoring categories, HIPAA controls, display labels, monitoring-only flags all from this table. Hardcoded `CATEGORY_CHECKS` in `db_queries.py` is fallback — registry overrides at startup via `load_check_registry()`. When adding new checks to daemon, add to registry migration. 15 guardrail tests verify completeness.
- **MONITORING_ONLY_CHECKS loaded from registry at startup.** Hardcoded set in `agent_api.py` is fallback. `load_monitoring_only_from_registry()` overrides.
- **Compliance scoring: latest-per-check, NOT last-N-bundles (Session 205).** Per unique `(check_type, hostname)`, take most recent result only. 24h staleness cutoff. LIMIT 200. Prevents score oscillation.
- **Recurrence-aware L2 escalation (Session 205).** 3+ same-type resolutions in 4h → bypass L1 → L2 with root-cause context. Recurrence checked BEFORE chronic (10+ in 7d). `escalation_reason` column on `l2_decisions`.
- **Time-travel reconciliation (Session 205, Phases 1-3.1).** Migration 160, `reconcile_events` append-only + RLS. Signals: `boot_counter_regression`/`uptime_cliff`/`generation_mismatch`/`lkg_future_mtime`. `MIN_SIGNALS_REQUIRED=2` wire-locked agent↔backend. Plan inline in checkin `reconcile_plan`, Ed25519-signed (same key as orders). Copy: "State Reconciliation" — NEVER "Time-Travel". Detail: `.agent/sessions/2026-04-12-session-205-time-travel-reconciliation.md`.
- **Reconcile sig verify = server's `signed_payload` byte-exact.** Never reconstruct client-side (Python `json.dumps(sort_keys=True)` adds `", "` in arrays; Go `json.Marshal` doesn't). Freshness uses `issued_at` from inside signed payload, not envelope.
- **Reconcile uses admin SQLAlchemy pool, not `tenant_connection()`.** Mirrors `/api/appliances/reconcile` posture; do NOT flip (breaks `reconcile_events` inserts).
- **`Processor.PurgeAllNonces()` persists empty map to disk.** Restart won't resurrect stale nonces.
- **Admin UI `/reconcile-events`.** `GET /api/admin/reconcile/events?site_id=&limit=`, mounted as `reconcile_admin_router` in main.py.
- **Migration auto-apply FAIL-CLOSED at startup (Session 205).** `main.py` lifespan → `migrate.cmd_up()`; `SystemExit(2)` if pending/failing. CI `set -e` + `N pending$` assertion. See `.agent/sessions/2026-04-12-session-205-cont-migration-hardening-fleet-outage.md` for the 90-min outage post-mortem.
- **`MIGRATION_DATABASE_URL` required on mcp-server.** Direct `mcp-postgres:5432` as `mcp` superuser (NOT `mcp_app` via PgBouncer — lacks schema-public CREATE). In repo `mcp-server/docker-compose.yml`.
- **`migrate.py cmd_up` uses `pg_advisory_lock(8675309)`.** Serializes concurrent replica startup.
- **`/api/admin/health` schema probe.** `{schema: {applied, pending}}`, `degraded` (HTTP 503) when pending > 0.
- **New migrations self-record in `schema_migrations` via `apply_migration`.** Pre-Session-205 files were backfilled once; don't manually INSERT rows.
- **Checkin transactional steps log at ERROR not WARNING.** 9 upgrades in `sites.py` STEPS 3.5/3.5b/3.6/3.6b/3.8b/3.9/4/4.x/4.5. Log shipper alerts on ERROR.
- **`nix.gc` + `nix.optimise` weekly automatic** in `/etc/nixos/configuration.nix` on VPS. `--delete-older-than 14d`, `persistent = true`.
- **asyncpg savepoint invariant (Session 205).** Every `conn.execute/fetch*` inside `tenant_connection`/`admin_connection` whose failure is caught non-fatally MUST be inside `async with conn.transaction():` (or SQLAlchemy `db.begin_nested()`). Python's try/except does NOT reset Postgres transaction state.
- **No silent write failures.** `except Exception: pass` on DB writes BANNED. `logger.warning` on DB failures BANNED → `logger.error(exc_info=True)`. Reads may eat exceptions; writes log-and-raise.
- **Audit-trigger allowlist.** DELETE/UPDATE-block triggers apply ONLY to audit-classified tables. Audit: admin_audit_log, client_audit_log, portal_access_log, incident_remediation_steps, partner_activity_log. Evidence (immutable, INSERT-only): compliance_bundles. Operational (no triggers): fleet_order_completions, fleet_orders, sessions, nonces, discovered_devices, go_agents. Migration 161 dropped overreach on fleet_order_completions.
- **Evidence INSERT-only — no DELETE upsert.** `evidence_chain.py submit_evidence` checks bundle_id first, returns idempotent dedup if present. ON CONFLICT incompatible with partitioned tables.
- **Checkin is a delivery contract.** HTTP 200 with empty `pending_orders` despite active fleet orders = credibility event. Target: Prom `msp_fleet_active_orders_undelivered{appliance_id}` alert > 0 for > 15min. Log shipper alerts on ERROR matching "transaction"/"savepoint"/"cleanup"/"Failed to fetch".
- **Persistence runbooks: RB-WIN-PERSIST-001 (tasks+XML+registry+WMI) and RB-WIN-PERSIST-002 (defender exclusions+GPO+tasks).** L2 recurrence recommends these over symptom-level fixes.
- **`configure_dns` fleet order** writes `extra_hosts` to config.yaml, restarts msp-dns-hosts.
- **Flywheel intelligence dashboard card + `/api/dashboard/flywheel-intelligence` endpoint.** Recurrence rate, chronic patterns, auto-promotions, cross-incident correlations. 3 background tasks: `recurrence_velocity_loop` (5min), `recurrence_auto_promotion_loop` (hourly), `cross_incident_correlation_loop` (hourly).
- **agent_api.py router is NOT registered in main.py** (too many overlapping endpoints). Individual endpoints like `/api/agent/l2/plan` are wired manually via `app.post()(handler)`.
- **L1 rule `incident_pattern` must have `incident_type` key.** The L1 query matches on `incident_pattern->>'incident_type'`. Synced/promoted rules with only `check_type` are dead weight.
- **CI/CD deploys code AND restarts automatically.** The deploy workflow rsyncs to `dashboard_api_mount`, restarts mcp-server+frontend, runs health check, and auto-rollbacks on failure. No manual restart needed.
- **iMac SSH on port 2222** (Session 191). Port 22 blocked by MikroTik HW offload (Ethernet↔WiFi). Use `ssh -p 2222 jrelly@192.168.88.50`. LaunchDaemon plist at `/Library/LaunchDaemons/com.local.sshd2222.plist`.
- **Credential encryption key required.** Fernet key at `/app/secrets/credential_encryption.key`. Missing key = RuntimeError on startup. Key also in `.env` as `CREDENTIAL_ENCRYPTION_KEY`.
- **`dashboard_api_mount` is the ONLY backend mount (Session 202).** The conflicting `./app/dashboard_api` build-context mount was removed. Deploy backend to `/opt/mcp-server/dashboard_api_mount/` only. Never rsync to `app/dashboard_api`.
- **Go build ldflags**: Version is `internal/daemon.Version`, NOT `main.Version`. Use `make build-linux VERSION=x` or `-X github.com/osiriscare/appliance/internal/daemon.Version=x`.
- **Fleet orders: complete old before creating new.** Active orders block delivery of newer orders. `UPDATE fleet_orders SET status='completed' WHERE status='active'` before creating.
- **Backend-authoritative mesh (Session 196, hardened 199).** Target assignment computed SERVER-SIDE in checkin (STEP 3.8c in sites.py). `hash_ring.py` uses round-robin when targets < 2x nodes (guarantees distribution), hash ring otherwise. Go daemon uses server assignments via `ApplyTargetAssignment()`, local ring is 15-min fallback only. 20 tests in `test_target_assignment.py`.
- **Terminology: "drift" is dead (Session 199).** User-facing text says "failing"/"compliance issue"/"configuration change". Variable names may still say "drift" — that's fine, only display text matters. `constants/status.ts` has `drifted.label: 'Failing'`. `cleanAttentionTitle()` in `constants/status.ts` maps backend titles.
- **Legal language (Session 199).** Never use "ensures", "prevents", "protects", "guarantees", "audit-ready", "PHI never leaves", "100%". Use "monitors", "helps detect", "reduces exposure", "audit-supportive", "PHI scrubbed at appliance". Disclaimers that LIMIT liability are safe — don't change them.
- **auth.py: always use execute_with_retry() (Session 199).** Raw `db.execute()` causes DuplicatePreparedStatementError through PgBouncer. `validate_session()` runs on EVERY authenticated request — must use `execute_with_retry()`.
- **Site rename is a multi-table migration.** When a `site_id` is renamed or recreated, ALL of these tables must be migrated in lockstep or drift bugs appear later: `site_credentials` (Session 199 — blank targets), `appliance_provisioning` (Session 210 — reflashed appliance re-provisioned under the old site_id, created a parallel `site_appliances` row), `aggregated_pattern_stats` (Session 210 — phantom `promotion_eligible=true` rows pointing at the renamed site), `platform_pattern_stats`, and any other table keyed by `site_id`. Prefer `UPDATE … SET site_id = $new WHERE site_id = $old` inside a single transaction across all of them; never rename the `sites` row alone.
- **Per-appliance signing keys (Session 196).** `site_appliances.agent_public_key` — NOT `sites.agent_public_key`. Evidence verification checks per-appliance keys. Multi-appliance sites MUST NOT use the single site-level key.
- **Appliance display_name (Session 196).** `site_appliances.display_name` auto-generated on checkin: first appliance = hostname, subsequent = `{hostname}-{N}`. Frontend shows `display_name || hostname || appliance_id`.
- **`compliance_bundles` is the evidence table, NOT `evidence_bundles`.** evidence_bundles is a legacy table (1 row). compliance_bundles has 232K+ entries, Ed25519 signed, hash-chained, OTS-anchored. **PARTITIONED by month** (Migration 138, Session 200). Default partition catches overflow. **ON CONFLICT (bundle_id) is INCOMPATIBLE with partitioned tables** — use DELETE+INSERT upsert pattern (Session 201).
- **`compliance_packets` table (Migration 141, Session 201).** Monthly compliance attestation packets persisted for HIPAA 6-year retention. Auto-generated on 1st of month at 02:00 UTC.
- **`mark_proof_anchored()` is the ONLY way to anchor OTS proofs (Session 201).** Single helper in evidence_chain.py handles ots_proofs, ots_merkle_batches, and admin_audit_log. Never write raw UPDATE ots_proofs SET status='anchored'.
- **Fleet orders: GET pending orders requires auth (Session 201).** `require_appliance_bearer` on `/{site_id}/appliances/{appliance_id}/orders/pending`. Was unauthenticated before Session 201.
- **Go daemon: dangerous orders blocked before server key received (Session 201).** `update_daemon`, `nixos_rebuild`, `healing`, `diagnostic`, `sync_promoted_rule` require Ed25519 verification. Only `force_checkin`, `run_drift`, `restart_agent` allowed pre-checkin.
- **Go daemon v0.3.84: ReloadRules() after sync_promoted_rule (Session 201).** Rules were being written to disk but never loaded into memory. `SetRuleReloader(d.l1Engine.ReloadRules)` wired in daemon.go.
- **Fleet is mixed-version: 2 appliances on v0.3.84, 1 (osiriscare-installer) still on v0.3.82.** Verify with `SELECT hostname, agent_version FROM site_appliances ORDER BY last_checkin DESC` before assuming uniform features. v0.3.82 is missing the rule reload + dangerous order blocking + nonce 2h fixes from Session 201.
- **Nonce replay TTL is 2 hours (Session 201).** Was 24h. `nonceMaxAge = 2 * time.Hour` in processor.go.
- **Download domain allowlist: api.osiriscare.net + release.osiriscare.net ONLY (Session 201).** github.com removed — too broad.
- **Checkin enforces auth_site_id from Bearer (Session 201).** `checkin.site_id = auth_site_id` — prevents appliance from spoofing another site's checkin. Never use `checkin.site_id` from request body for auth decisions.
- **Partner/client login lockout (Session 201).** 5 failed attempts → 15-min lockout. Columns: `failed_login_attempts`, `locked_until` on partners + client_users (migration 143). Reset on successful login.
- **Session token hashing: shared.hash_session_token() is the source of truth (Session 201).** Partner and client portals delegate to `shared.py`. Don't create new hash_token() functions — use the shared one.
- **Incident dedup uses ON CONFLICT (dedup_key) (Session 201).** Partial unique index on `dedup_key WHERE status NOT IN ('resolved','closed')`. Prevents race condition when two appliances report same issue simultaneously.
- **Alert digest: never use RETURNING COUNT(*) (Session 201).** PostgreSQL doesn't allow aggregate functions in RETURNING. Use `conn.execute()` and parse the result string instead.
- **`portal_access_log` is PARTITIONED by month** (Migration 138, Session 200). 
- **`incident_remediation_steps` replaces `incidents.remediation_history` JSONB** (Migration 137, Session 200). Relational table: incident_id FK, tier, runbook_id, result, confidence, created_at. `remediation_history` JSONB column still exists on incidents but is no longer written to — code uses INSERT/SELECT on new table. routes.py falls back to JSONB if table doesn't exist.
- **Dual connection pools are intentional.** SQLAlchemy (shared.py, pool_size=20) for admin CRUD via `Depends(get_db)`. asyncpg (fleet.py, min=2/max=25) for RLS-enforced queries via `tenant_connection()`/`admin_connection()`. Both go through PgBouncer (25 server conns). Cannot consolidate — RLS requires asyncpg's `SET LOCAL` transaction control.
- **Checkin savepoints: every step must have one.** Bare queries in `sites.py` checkin handler poison the tenant_connection transaction on failure. All steps (3.5, 3.6, 4, 4.5, 6b-2) wrapped in `async with conn.transaction():` for isolation (Session 200).
- **`normalize_mac_for_ring()` vs `normalize_mac()`.** `hash_ring.py` has `normalize_mac_for_ring()` (stripped, no separators: `843A5B91B661`). `sites.py` has `normalize_mac()` (colon-separated: `84:3A:5B:91:B6:61`). Don't confuse them.
- **Device sync has 3 competing sources.** Netscan (6 devices, IP IDs), replay (12 devices, UUID IDs), home net (23 devices, MAC IDs). The CASE expression in device_sync.py ensures IP format always wins. GREATEST() prevents timestamp reversion.
- **CSRF exempt paths for machine-to-machine endpoints.** `/api/witness/submit`, `/api/provision/`, `/api/appliances/checkin`, `/api/devices/sync` must be in `csrf.py EXEMPT_PATHS`. Missing = 403/500 from daemon.
- **Ops center endpoints**: `/api/ops/health` (admin), `/api/ops/health/{org_id}` (partner), `/api/ops/audit-readiness/{org_id}`, `PUT /api/ops/audit-config/{org_id}`. Registered in main.py via ops_health_router + audit_report_router.
- **requirements.txt MUST use exact pins (`==`).** Loose pins (`>=`) caused pydantic v2 breakage in CI. Include `pydantic-core` and `pydantic-settings` explicitly — they're transitive deps that pip resolves differently across Python versions.
- **agent_api.py `_enforce_site_id()` required on ALL appliance endpoints (Session 202).** Every endpoint with `Depends(require_appliance_bearer)` must call `_enforce_site_id(auth_site_id, request_site_id, endpoint_name)`. Prevents cross-site spoofing. 13 endpoints hardened.
- **`execute_with_retry()` required for ALL SQLAlchemy queries through PgBouncer (Session 202).** auth.py (7 queries), routes.py (104 queries), oauth_login.py (25 queries), users.py (33 queries) all migrated. New code must use `execute_with_retry(db, text(...), params)` not `db.execute()`.
- **SQLAlchemy async event binding: target `AsyncSession.sync_session_class` (the class), NOT `sessionmaker_instance.sync_session_class` (Session 209, 2026-04-18).** `async_sessionmaker` instances do NOT have a `sync_session_class` attribute — binding that way raises AttributeError, which a surrounding `try/except Exception: pass` swallows silently. The `after_begin` listener then never fires and RLS fail-closed bites the whole SQLAlchemy tree. This pattern caused the 2026-04-18 RLS P0 (2,608 InsufficientPrivilegeError rejections in 2h on `compliance_bundles` INSERT). Fixed in commits `ebb9f17`+`2ddc596`. Rule: narrow silent-swallow handlers to `ImportError/AttributeError` ONLY (for test-stub paths) — never bare `Exception`.
- **Admin context for SQLAlchemy sessions uses `SET LOCAL` + `after_begin` (Session 209).** `shared.py` binds `@event.listens_for(AsyncSession.sync_session_class, "after_begin")` that issues `SET LOCAL app.is_admin = 'true'` at the start of every transaction. Session-level `SET` is wiped by PgBouncer's `DISCARD ALL` between transactions — only `SET LOCAL` survives because it's transaction-scoped and DISCARD ALL runs BETWEEN transactions. Migration 234 relies on this listener being wired correctly; verify with `docker exec -w /app/dashboard_api mcp-server python3 -c "import asyncio, shared; from sqlalchemy import text; asyncio.run((lambda: (lambda db: (lambda: None)())(shared.async_session()))())"` or a simple `SELECT current_setting('app.is_admin', true)` inside a session — must return `'true'`, not `'false'`.
- **`evidence_chain_stalled` sev1 substrate invariant (Session 209).** Fires when ≥1 appliance checked in in the last 15 min but 0 `compliance_bundles` inserted in that window. Catches evidence-chain INSERT failures at the outcome layer regardless of root cause (RLS, missing partition, signing key rotation, disk pressure). Total invariant count: 32 (was 28+). Added to three-list lockstep: `ALL_ASSERTIONS` + `_DISPLAY_METADATA` + forward-ref lambda.
- **Flywheel event_type three-list lockstep (Session 209, migration 236).** The ledger `promoted_rule_events.event_type` CHECK, `flywheel_state.EVENT_TYPES` frozenset, and the `promoted_rule_lifecycle_transitions` matrix MUST stay in lockstep. Drift silently kills the entire Flywheel: every `safe_rollout_promoted_rule` advance raises `CheckViolationError`, which falls into a `logger.warning` (now `logger.error`), and the fleet_order is issued but the ledger never reflects it. This is what caused the 2026-04-18 "Flywheel Intelligence all-zeros" finding (17/43 promoted_rules orphaned from the ledger). CI gate: `tests/test_three_list_lockstep_pg.py`. Substrate invariant: `flywheel_ledger_stalled` (sev1) alerts when rolling_out rules have fleet completions but 0 ledger writes in a 60 min window.
- **Platform auto-promotion must go through the spine (Session 209, main.py Step 4).** After `INSERT INTO l1_rules` with `source='platform'`, the loop MUST also: (1) INSERT one `promoted_rules` row per active site, (2) `advance_lifecycle(rule_id, 'approved', 'promotion_approved', ...)`, (3) `safe_rollout_promoted_rule(scope='fleet', ...)` which issues `sync_promoted_rule` fleet_orders per site AND writes the `rollout_issued` ledger event. Pre-fix: platform rules sat dormant in `l1_rules` with zero appliances informed. Post-fix: every cross-org auto-promotion lands on every appliance + shows up on the Flywheel Intelligence dashboard.
- **`_flywheel_promotion_loop` Step 5 was REMOVED (Session 209).** Previously it inlined `UPDATE l1_rules SET enabled=false` and `UPDATE l1_rules SET source='synced'` — bypassing `advance_lifecycle()` and competing with the orchestrator. `CanaryFailureTransition` + `GraduationTransition` in `flywheel_state.py` own those state changes now. One state owner per transition. Never re-add direct `UPDATE l1_rules` for lifecycle purposes.
- **`safe_rollout_promoted_rule(scope='fleet')` is the fleet-wide rollout primitive (Session 209).** Default `scope='site'` preserves the Session-206 call sites (`promote_candidate`, `learning_api.bulk_promote`, `client_portal.approve`). `scope='fleet'` iterates `site_appliances WHERE deleted_at IS NULL` and issues one `sync_promoted_rule` fleet_order per site. Used by platform auto-promotion only — per-partner/per-site paths still use `scope='site'`.
- **`partition_maintainer_loop` (Session 209, background_tasks.py).** Daily (86400s) creates the next 3 months of `promoted_rule_events_YYYYMM` partitions idempotently via `CREATE TABLE IF NOT EXISTS ... PARTITION OF`. 10-min startup delay. Prevents the "month rolled over, INSERT into default partition, OOM-risk" class of failure.
- **No silent write failures in the flywheel loop (Session 209).** All 7 Step try/except handlers (0, 1, 2, 3, 4, 6, plus Step 4 candidate-loop) now `logger.error("<step>_failed", exc_info=True, extra={"exception_class": type(e).__name__})`. `logger.warning` is banned for DB write failures per the Session-205 rule — every log line is greppable by shipper for alerting.
- **Go daemon uses `slog` structured logging (Session 202).** 15 files migrated from `log.Printf` to `slog.Info/Warn/Error` with `"component"` key. New Go code must use slog, not log.Printf.
- **`go_agents.site_id` has FK constraint to `sites(site_id)` ON DELETE CASCADE (Migration 144).** Prevents orphaned agents under wrong sites.
- **Go agent summary computed live on read (Session 202).** `get_site_go_agents()` in sites.py computes summary from raw `go_agents` rows, not from stale `site_go_agent_summaries` table.
- **`COMPLIANCE_CATEGORIES` in `client_portal.py` is the single source of truth** for check-type → category mapping. Never inline category dicts in functions.
- **`_send_smtp_with_retry()` in `email_alerts.py`** is the single SMTP send function. All email functions delegate to it. Never write inline SMTP retry loops.
- **asyncpg `$1` parameters need `::text` cast** when the same connection runs multiple queries with `$1` against different column types. PgBouncer statement caching causes `AmbiguousParameterError` otherwise. See `health_monitor.py` mesh isolation queries.
- **`client_audit_log` is append-only via PostgreSQL trigger (Migration 149, Session 203).** UPDATE/DELETE blocked at the DB level. HIPAA §164.528 disclosure accounting + §164.316(b)(2)(i) 7-year retention. Read via `GET /api/client/audit-log` (1..2555 day lookback). Never write raw INSERTs — go through `_audit_client_action()` in `client_portal.py`.
- **`_audit_client_action()` is the single source of truth for client mutation auditing (Session 203).** All 13 client mutations call it with `request: Request` (for IP capture), action name, target, and details JSONB. Alert handler uses dynamic `f"ALERT_{action.upper()}"`. New client mutations MUST call this helper.
- **Partner audit goes through `partner_activity_logger.py` (Session 203).** Don't create a parallel `partner_audit_log` table — Migration 150 dropped that. Extend `PartnerEventType` enum and use `record_partner_event()`.
- **`process_merkle_batch()` MUST append a random suffix to `batch_id` (Session 203).** Pre-`965dd36` code used `f"MB-{site_id[:20]}-{batch_hour.strftime('%Y%m%d%H')}"` which collided when called twice in same hour. Fix: `+ f"-{secrets.token_hex(4)}"`. ON CONFLICT kept as belt-and-suspenders. Migration 148 backfilled 1,198 affected bundles to `legacy`. Regression test in `test_merkle_batch_id_uniqueness.py`.
- **Auditor verification kit ZIP at `/api/evidence/sites/{site_id}/auditor-kit` (Session 203).** Public refugees endpoint. `download_auditor_kit()` in `evidence_chain.py` builds in-memory ZIP via `io.BytesIO` + `zipfile`. ZIP contents: README.md + verify.sh (no `osiriscare.net` calls — regression-tested) + chain.json (with disclosures inline) + bundles.jsonl + pubkeys.json (with SHA256 fingerprints) + ots/{bundle_id}.ots files. Range cap 1..5000. Guarded by `require_evidence_view_access`.
- **Public Merkle disclosure at `docs/security/SECURITY_ADVISORY_2026-04-09_MERKLE.md` (Session 203).** OSIRIS-2026-04-09-MERKLE-COLLISION. Embedded inline in every `chain.json` shipped in the auditor kit. Disclosure-first credibility commitment — every future evidence-integrity event publishes the same way.
- **`/recovery` is a public landing page for compliance refugees (Session 203).** `frontend/src/pages/RecoveryLanding.tsx`. Lazy-loaded route. Positioning: "Real evidence. Cryptographically provable. Verified on your auditor's laptop." Avoids naming Delve/DeepDelver directly.
- **Vite Web Worker imports MUST use `?worker` query, NOT `new URL()` (Session 203 Tier 2.1).** `import VerifyChainWorker from './verifyChainWorker.ts?worker'` then `new VerifyChainWorker()`. The `new URL(..., import.meta.url)` pattern silently copies the .ts source verbatim into dist/ instead of compiling — browsers can't execute TypeScript and the worker fails in production. Verified by inspecting `dist/assets/verifyChainWorker-*.js` after build (must be ASCII JS, ~9KB, not 314 lines of TS source).
- **`verifyChainWorker.ts` walks the FULL evidence chain (Session 203 Tier 2.1).** Replaces the 10-bundle limitation in Batch 5's `useBrowserVerify`. Walks in batches of 200 via the streamed `/sites/{id}/bundles?include_signatures=true&order=asc` endpoint. Tracks `prevBundleHash` across batch boundaries to verify cross-batch chain linkage. Reports incremental progress + final summary. Used by `useBrowserVerifyFull` + `FullChainVerifyPanel`.
- **`/sites/{id}/bundles` requires `include_signatures=true` for browser verification (Session 203 Tier 2.1).** Default response hides `agent_signature` and `chain_hash` to keep admin UI payload small. Browser verifier MUST pass `include_signatures=true&order=asc`. The Batch 5 hook was silently no-op'ing because it expected those fields but they weren't in the default response.
- **Packet determinism is regression-tested (Session 203 Tier 2.2).** `test_packet_determinism.py` generates two CompliancePackets for the same site/period and asserts byte-identical markdown after stripping `Generated:` timestamp. The "no-Delve canary" test generates packets for two DIFFERENT sites with stubbed data and asserts SHA256 of the bodies differs. Any future regression toward template-only output (the Delve playbook) fails CI.
- **`/api/evidence/sites/{id}/random-sample?count=N&seed=K` for auditor spot-checks (Session 203 Tier 2.4).** Caps at count=100 to prevent bulk export abuse. `seed` is mapped from int → `[-1.0, 1.0]` via `((seed % 2_000_001) - 1_000_000) / 1_000_000.0` and passed to `setseed()` so two requests with the same seed return the same bundles. Returns full agent_signature + chain_hash payload. Legacy bundles ARE included (auditor's job is to confirm they exist and are honestly labeled).
- **`/api/evidence/sites/{id}/bundles/{bundle_id}/ots` returns raw .ots file bytes (Session 203 H3).** Per-bundle OpenTimestamps proof download. Returns 404 for legacy/pending bundles. Response headers include `X-Bundle-Hash`, `X-OTS-Status`, `X-Calendar-URL` so an auditor can sanity-check what they downloaded. UI button on each `BundleTimeline` row in `PortalVerify`, only shown for `anchored` or `verified` ots_status.
- **Public changelog at `/changelog` (Session 203 Tier 2.6).** `frontend/src/pages/PublicChangelog.tsx`. Edit by adding entries to the top of the `ENTRIES` array. Categories: security/feature/fix/disclosure. Auditors read this page — keep summaries truthful and conservative.
- **`appliance_framework_configs` is the site-level framework config table (Session 203 Tier 3 H6/H8).** Has `enabled_frameworks` (array), `primary_framework`, `industry`, `framework_metadata`. Read via `_get_site_framework_info(db, site_id)` in portal.py — falls back to HIPAA when missing. Backend supports 9 frameworks: hipaa, soc2, pci_dss, nist_csf, cis, sox, gdpr, cmmc, iso_27001 (plus nist_800_171). The portal UI used to be hardcoded to HIPAA — now uses `data.frameworks?.primary_label` everywhere.
- **`/api/portal/site/{id}` ships a `frameworks` payload (Session 203 Tier 3).** `PortalFrameworks` model: `primary`, `primary_label`, `enabled[]`, `enabled_labels[]`. Frontend `PortalScorecard` uses it for hero text, header summary, auditor section title, and dynamic column label (HIPAA Code / SOC 2 Trust Service / PCI DSS Req / NIST Function). Multi-framework chip list renders when `enabled_labels.length > 1`.
- **`/api/partners/me/audit-log` is the partner self-service disclosure accounting endpoint (Session 203 Tier 3 H7-partner).** Mirrors `/api/client/audit-log`. Filter by category, lookback window 1..2555 days (HIPAA §164.316(b)(2)(i) max). Scoped to authenticated partner via `require_partner_role`. Frontend page at `/partner/audit-log` (`PartnerAuditLog.tsx`) — humanizes 30+ event types via `EVENT_LABELS` map.
- **Chain Status badge shows legacy ratio when present (Session 203 Tier 3 H1).** PortalScorecard "Chain Status" badge no longer says "Valid" while 22.6% of bundles are legacy. New format: `Valid · 77% signed` with amber tone when `legacyCount > 0`. Dedicated warning panel below the metric grid links the Merkle disclosure (`OSIRIS-2026-04-09-MERKLE-COLLISION`).
- **PublicKeysPanel renders per-appliance pubkeys in-app (Session 203 H2).** `frontend/src/portal/PublicKeysPanel.tsx`. Wired into `PortalVerify` next to the FullChainVerifyPanel. Computes SHA-256 fingerprints in the browser via `crypto.subtle.digest` (must match the auditor kit format — 16 hex chars). Has copy + download pubkeys.json buttons. The auditor kit ZIP is still the canonical handoff; this panel exists for auditors who look in-app first.
- **Source-level pytest tests for TSX files (Session 203 pattern).** Frontend Vitest covers behavior; Python source-level tests cover existence + structural guard rails (e.g., "the genesis panel must contain the literal 64-zero sentinel"). They prevent silent deletion of UI sections during refactors. See `test_full_chain_browser_verify.py`, `test_genesis_panel_and_changelog.py`, `test_tier3_audit_close.py`, `test_h2_h3_inapp_downloads.py`.
- **Migration 151: DELETE triggers on evidence + audit tables (Session 204).** `prevent_audit_deletion()` blocks DELETE on: `compliance_bundles` (all partitions), `admin_audit_log`, `client_audit_log`, `incident_remediation_steps`, `fleet_order_completions`, `portal_access_log`. `prevent_completed_order_modification()` blocks UPDATE on `fleet_orders` where `status='completed'`. Retention cleanup requires manual DBA intervention — no automated DELETE.
- **Evidence submit enforces auth_site_id (Session 204).** `evidence_chain.py` now checks `site_id != auth_site_id` before processing. Previously, an appliance with site-A token could submit evidence to site-B.
- **SSO user lookup scoped to org_id (Session 204).** `client_sso.py` query changed from global email lookup to `WHERE email = $1 AND client_org_id = $2`. Prevents cross-org authentication via SSO.
- **Device sync hostnames scrubbed at egress (Session 204).** `netscan.go` applies `phiscrub.Scrub()` to `deviceSyncEntry.Hostname`. Previously, discovered device hostnames (potentially patient-named like "PATIENT-ROOM-201-PC") were sent unscrubbed to Central Command.
- **Deploy result hostnames + errors scrubbed (Session 204).** `daemon.go` scrubs `DeployResult.Hostname` and `.Error` before checkin. Error messages may contain raw hostnames from failed deployments.
- **HIPAA 7-year retention enforced (Session 204).** The 3-year (1095-day) automated DELETE from `admin_audit_log` in `alert_router.py` was removed. HIPAA §164.316(b)(2)(i) requires minimum 6–7 year retention. DELETE triggers now enforce immutability.
- **Fleet order health check rollback (Session 204 → 207).** Old: completion ACK fired before 70s health check, so a rolled-back upgrade left `agent_version` lagging silently. **FIXED daemon 0.4.3 (Session 207, commit `46eea2b`)**: handler writes `<stateDir>/pending-update.json` marker, returns `status="update_pending"` sentinel, processor skips auto-complete. New `Processor.CompletePendingUpdate(ctx, currentVersion)` runs at startup, sleeps 90s past the marker write so the health check has time to roll back, then posts /complete success or rollback-failure based on Version match. Marker is atomic-write JSON with absolute `TimeoutAt`; multi-restart safe. Tests: 4 cases in `appliance/internal/orders/pending_update_test.go`. Binary published `/var/www/updates/appliance-daemon-0.4.3` sha `39c89588dd...`. Use `api.osiriscare.net` (NOT `release.osiriscare.net` — no DNS A record exists; `fleet_cli.py create` now refuses unresolvable URLs).
- **Fleet CLI must include `--param site_id=<site>` for v0.3.82 compatibility.** The April 6 order that worked on the installer included `site_id` in parameters. Orders without it are silently dropped by v0.3.82 daemons (unconfirmed but correlates with observed behavior).
- **Flywheel Spine (Session 206, migration 181).** `promoted_rules.lifecycle_state` is the authoritative state field — 9 states (`proposed`,`shadow`,`approved`,`rolling_out`,`active`,`regime_warning`,`auto_disabled`,`graduated`,`retired`). All transitions flow through `advance_lifecycle(rule_id, new_state, event_type, actor, stage, proof, reason, site_id, outcome)` — the ONLY sanctioned mutation path. A trigger blocks direct UPDATE of `lifecycle_state` (tamper-evident; DBA-only bypass via `SET LOCAL app.allow_lifecycle_bypass='true'`). Every transition writes an append-only row to `promoted_rule_events` (partitioned monthly, DELETE+UPDATE blocked). Three lists must stay in lockstep for any new event_type or state: CHECK on `promoted_rule_events.event_type`, CHECK on `promoted_rules.lifecycle_state`, seeds in `promoted_rule_lifecycle_transitions`.
- **Flywheel orchestrator replaces fragmented Step-5 in flywheel_promotion_loop.** `flywheel_state.run_orchestrator_tick(conn, enforce=True|False)` runs 5 transitions (RolloutAcked, CanaryFailure, RegimeAbsoluteLow, Graduation, ZombieSite). Each transition has its own try/except with `logger.error(exc_info=True)` + Prom counter — one failure cannot mask another. Background loop `flywheel_orchestrator_loop` runs every 5 min. Mode controlled by `FLYWHEEL_ORCHESTRATOR_MODE=shadow|enforce` (default `shadow`). Cutover: env var + `docker compose restart mcp-server`.
- **`safe_rollout_promoted_rule` is the single rollout entrypoint.** All 3 promotion writers (`promote_candidate`, `learning_api.bulk_promote`, `client_portal.approve`) delegate here. On success it calls `advance_lifecycle(approved→rolling_out)` so the ledger reflects the rollout. Illegal transitions log WARNING (not ERROR) — the fleet_order write itself is authoritative.
- **Deploy bug (Session 206, fixed).** `docker compose up -d` alone is a no-op when compose config hasn't changed — Python code was bind-mounted but the running process kept old modules. 6h of silent "green" deploys today. Fix: workflow runs `docker compose restart mcp-server frontend` after `up -d`, PLUS `/api/version` verify step that asserts `runtime_sha == disk_sha == GITHUB_SHA`. Either mismatch → CI fails. RELEASE_SHA stamped inside `dashboard_api/` so atomic rsync carries it to `/app/dashboard_api/RELEASE_SHA` (bind-mounted path).
- **pgbouncer DuplicatePreparedStatement on restart (FIXED Session 206, 850b17f).** When mcp-server restarts through pgbouncer's transaction pool, asyncpg's deterministic `__asyncpg_stmt_N__` names used to collide with stmts left by prior process. Deploy workflow now runs `pg_terminate_backend` on all mcp/mcp_app sessions + restarts pgbouncer BEFORE restarting mcp-server, flushing the prepared-stmt namespace. Note: asyncpg 0.30 does NOT expose `prepared_statement_name_func` (a6fc253 tried that kwarg — was a no-op, reverted in 8013268).
- **Substrate Integrity Engine (Session 207, mig 207).** `assertions.py` runs every 60s in `main.py` task supervisor as `substrate_assertions`. 11 named invariants (legacy_uuid_populated, install_loop, offline_appliance_over_1h, agent_version_lag, fleet_order_url_resolvable, discovered_devices_freshness, install_session_ttl, mesh_ring_size, online_implies_installed_system, every_online_appliance_has_active_api_key, auth_failure_lockout). UPSERTs into `substrate_violations` with open-row dedup on (invariant_name, COALESCE(site_id, '')) WHERE resolved_at IS NULL; auto-resolves rows whose violations clear. View `v_substrate_violations_active` powers `/admin/substrate-health`. Adding a new invariant: one CheckFn returning `List[Violation]` + one entry in `ALL_ASSERTIONS` — no schema, no deploy gymnastics.
- **Migration 208 row-guard admin bypass (Session 207).** Migration 192 row-guard now also bypasses when `current_user='mcp'` (admin/migrator role). Application code (`mcp_app`) still must use a per-row filter or `SET LOCAL app.allow_multi_row='true'`. Eliminates the entire "I forgot the bypass in migration N" CI failure class. Two failures in 24h before this fix.
- **Migration 209 api_keys triggers (Session 207).** Two centralized triggers replace ad-hoc per-endpoint discipline. (1) BEFORE INSERT auto-deactivates prior active rows for the same `(site_id, appliance_id)` (treats NULL appliance_id as its own site-level bucket). (2) AFTER INSERT/UPDATE/DELETE writes a structured row to `admin_audit_log` (action=`api_key.<op>`, target=`api_key:<id>`, username=current_user). Closes the leaked-active-keys class (observed: 7C:D3 had 3 concurrent active rows) and the no-audit-trail class. Cleanup pass collapses pre-existing dupes idempotently.
- **fleet_cli URL DNS validation (Session 207).** `fleet_cli.py create update_daemon` now `socket.gethostbyname(urlparse(binary_url).hostname)` before INSERT and refuses unresolvable hosts. Observed 2026-04-14: a fleet order pointing at `release.osiriscare.net` (no A record anywhere in public DNS) DNS-failed every 60s on the canary for an hour with no visible alert. Use `api.osiriscare.net/updates/<binary>` — `release.osiriscare.net` does not exist.
- **install_sessions loop alert (Session 207).** `health_monitor._check_install_loops()` opens a Sev-2 notification when any MAC has `install_sessions.checkin_count > 5` at stage `live_usb`. Also surfaced as substrate invariant `install_loop`. Catches the failure mode where the BIOS can't boot the installed system and the box re-boots into the USB forever (observed: t740 hit 50 checkins with no alert pre-fix).
- **Daemon auto-rekey (already in v0.3.84+; legacy bridge in Session 207).** On 3+ consecutive 401/AUTH_KEY_MISMATCH responses, daemon calls `attemptRekey` which POSTs to `/api/provision/rekey` with site_id+mac and rewrites `/var/lib/msp/config.yaml` atomically. Daemons < v0.3.84 lack this — use `mcp-server/central-command/backend/scripts/recover_legacy_appliance.sh <site_id> <mac> <ip>` to mint a key + push it via SSH using the MAC-derived emergency password (`osiris-<sha256("osiriscare-emergency-<mac>")[0:8]>`).
- **Installer v25 BOOTX64.EFI fallback + v26 hardware-compat gate (Session 207).** v25 installer copies `\EFI\systemd\systemd-bootx64.efi → \EFI\BOOT\BOOTX64.EFI` after dd, fixing UEFI fallback for HP thin clients (t740/t640) that don't auto-scan ESP without an NVRAM entry (raw image is `canTouchEfiVariables=false` because dd'd onto many machines). v26 adds `iso/supported_hardware.yaml` + a pre-flight gate that halts on uncertified DMI product. Initial matrix: HP t640, HP t740. Adding a model is a 5-line PR with install evidence.
- **Provisioning latency SLA (Session 207).** `/api/dashboard/admin/substrate-installation-sla` returns p50/p95/p99 minutes from `install_sessions.first_seen` to `site_appliances.first_checkin`. Target: 99th-percentile under 5 min. Surfaced on the Substrate Health panel.
- **Recovery-shell escape hatch (Session 207, mig 223, ISO v34).** `enable_recovery_shell_24h` fleet order: operator pubkey → watchdog writes `/etc/msp-recovery-authorized-keys` + `systemctl start sshd` + arms systemd-run transient timer for 1..24h that stops sshd + wipes keys on expiry. Installed-system sshd is in the NixOS closure (`services.openssh.enable = true`) but `systemd.services.sshd.wantedBy = []` so it does NOT autostart on boot. Added to three-list lockstep.
- **Break-glass retrieval hardened (Session 207 R+S follow-ups).** GET `/api/admin/appliance/{aid}/break-glass` is rate-limited to **5/hr** via extended `check_rate_limit(window_seconds, max_requests)`. Reason validator rejects < 20 chars, < 5 distinct chars, or no alphabetic word (kills "aaaaaaaaaaaaaaaaaaaa" bypass). Submit uses `INSERT ... ON CONFLICT (appliance_id) DO UPDATE`. Retrieval writes `break_glass_passphrase_retrieval` attestation bundle → auditor kit. Event added to `ALLOWED_EVENTS` only (NOT fleet_cli `PRIVILEGED_ORDER_TYPES` — it's an admin API call, not a queued fleet order; lockstep checker permits this asymmetry).
- **Billing is PHI-free by design (Session 207, mig 224).** Stripe is NOT a Business Associate. Enforced at three layers: (1) `subscriptions` table CHECK-constraint comment forbids column names matching `patient|phi|treatment|diagnosis|provider_npi`; (2) `client_signup.py` whitelists Stripe `customer.metadata` to `{email, practice_name, signup_id, plan, state}` only; (3) `/signup/baa` requires SHA256-committed acknowledgment BEFORE Checkout session creation. Client path lives in `client_signup.py`; partner path stays in `billing.py`. Shared webhook at `/api/billing/webhook` dispatches by `metadata.signup_id` (client) vs `metadata.partner_id` (partner).
- **Stripe products use lookup_keys, not price IDs.** Code references `osiris-pilot-onetime`, `osiris-essentials-monthly`, `osiris-professional-monthly`, `osiris-enterprise-monthly`. `stripe.Price.list(lookup_keys=[...])` resolves at runtime. Price-id rotation in Stripe doesn't require a code deploy.
- **`stripe` Python lib in image requires manual rebuild.** Deploy workflow does NOT rsync Dockerfile or requirements.lock — only rsyncs backend/frontend code. New/bumped Python deps require `scp` of Dockerfile + requirements.lock to `/opt/mcp-server/app/` on VPS and `docker compose build mcp-server && docker compose up -d mcp-server`. Container `appuser` is pinned to **UID 1000** to match host bind-mount ownership (secrets/, runbooks/, evidence/ on VPS are uid=1000).
- **BAA signatures append-only (mig 224).** `baa_signatures` table has UPDATE + DELETE trigger blocking modifications. 7-year HIPAA §164.316(b)(2)(i) retention. Every e-sign records signer_name + IP + UA + BAA_VERSION (in code) + SHA256 of the acknowledgment text shown. Old signatures remain bound to their original hash even after BAA text updates.
- **Pilot CTA is self-serve; paid-tier CTAs are demo-first.** `Pricing.tsx` pilot button routes `/signup?plan=pilot`. `$499/$799/$1299` tier buttons still route to Calendly. PM consensus: healthcare-SMB paid-tier buyers warrant sales-assisted qualification; $299 pilot is low enough ACV for self-selection.
- **`provisioning_stalled` invariant (Session 208, commit 96ce738).** Fires when a MAC appears in `install_sessions` within the last hour (`checkin_count ≥ 3`) but `site_appliances.last_checkin` is NULL or > 15 min stale. Details JSONB carries the DNS-filter-whitelist hint naming common culprits (Pi-hole, Umbrella, Fortinet, Sophos, Barracuda). Surfaces on `/admin/substrate-health`. Fires EARLIER than `installed_but_silent` (15 min vs 20 min) so the signal hits while the installer is still loopable + diagnosable. Invariant count now 28.
- **ApplianceCard staleness (Session 208, commit 96ce738).** When `site_appliances.live_status != 'online'`, IP / uptime / agent version / nixos version fields render with line-through + muted color and a `(frozen Xm ago)` annotation. Backend `live_status` remains source of truth — never re-derive staleness client-side. `Last Checkin` field itself is NOT wrapped (it's the real-time field). Eliminates the "Uptime: 2m / Last Checkin: 41m ago" contradiction that sent the April-16 t740 debug chasing subnet/NAT red herrings for 20 min.
- **SEO canonical + deploy static/ gap fix (Session 208, commit e5f95db).** `frontend/index.html` no longer carries a hardcoded `<link rel="canonical">`; SPA routes each call `setCanonicalAndDescription()` in their useEffect (homepage explicitly sets `/` via `setHomepageCanonical()`). Deploy workflow rsyncs `sitemap.xml` + `robots.txt` from `frontend_dist/` to `/opt/mcp-server/static/` during atomic switch AND rollback — Caddy serves those files from `static/`, not from `frontend_dist/`. CI Verify step fails if byte sizes drift.
- **`JsonLd` helper must escape `</` (Session 208).** `frontend/src/components/marketing/JsonLd.tsx` renders the payload as a `<script type="application/ld+json">` text child and replaces every `<` with `\u003c` — raw `</` in a JSON-LD payload lets a browser parser close the script tag early. Tests in `src/components/marketing/marketing.test.tsx` assert the raw `</` never appears and the escaped form does.
- **Auditor kit rate-limited (Session 208, commit eed0958).** `GET /api/evidence/sites/{site_id}/auditor-kit` is capped at **10/hr** via `check_rate_limit(site_id, 'auditor_kit_download', window_seconds=3600, max_requests=10)`. 429 with `Retry-After` header on exceed. The helper signature is now `(site_id, action, window_seconds?, max_requests?)` — earlier callers that used default 5/15min still work. Import in `evidence_chain.py` uses a try/except for relative vs direct-module pytest compat (same pattern as `auth.py`).
- **`upgrade_pending_proofs` logs at ERROR (Session 208, commit eed0958).** `evidence_chain.py` OTS upgrade loop formerly `logger.warning(... exc_info=True)` → now `logger.error(..., exc_info=True, extra={"bundle_id":..., "calendar_url":..., "exception_class": type(e).__name__})`. Inner savepoint failure logs a "double failure" error. Log shipper alerts key off ERROR for this subsystem; pre-fix failures would have been silent.
- **OTS tamper property tests (Session 208, commit eed0958).** `mcp-server/central-command/backend/tests/test_ots_tamper_property.py` — 13 pynacl-based tests (TestBaseline x2, TestTamperDetection x8, TestInvariants x3). Mutates every byte of chain_hash (64 positions), bundle_hash, prev_hash; tampers chain_position, signed_data, signature, pubkey, content-swap. `GENESIS_PREV_HASH = "0" * 64` and `_compute_chain_hash` mirrors production (`SHA256(f"{bundle_hash}:{prev_hash or 'genesis'}:{chain_position}")`).
- **Landing page mesh-scoped positioning (Session 208, commit ebc4ee4).** Hero copy: "single clinic to a multi-site provider network or DSO" — NOT "small practices". Nine-framework + mesh + cryptographic-evidence are the three differentiators. `/2026-hipaa-update`, `/for-msps`, `/pricing`, `/blog` in primary nav. 2026-Ready teal ribbon + NPRM 3×3 grid + three-tier scale section. Rendered React content must match crawler-visible HTML fallback. Confirmed visible via `curl https://www.osiriscare.net/assets/LandingPage-*.js | grep "2026-Ready"`.
- **v38 audit kill-switch (Session 208, ISO v38).** `modules/compliance-agent.nix:852` is now `security.auditd.enable = mkDefault false` and `iso/appliance-disk-image.nix:278` kernelParams include `"audit=0"`. Root cause: execve-rule auditd on HP t740-class hardware produced events faster than kauditd could drain → backlog overflow → console spam → userspace starvation → sshd + appliance-daemon never came up. Every installed system from v25–v37 carried this bug; v38 closes it on the installed-system path (live ISO already did via `mkForce false` at `iso/appliance-image.nix:235`). Re-enable auditd per-host only after setting `kernel.audit_backlog_limit=8192`. Evidence chain + OTS proofs cover HIPAA attestation without kernel audit.
- **v38 install halt telemetry (Session 208, migration 226).** New bash helper `post_halt_report()` in `iso/appliance-image.nix:698` POSTs `{installer_id, installer_version, halt_stage, halt_reason, hw_product, bios_vendor, bios_version, log_tail}` to `/api/install/report/halt`. Called BEFORE every `sleep 86400` path in `check_hardware_compat`. New Pydantic model `InstallReportHalt` + handler `post_install_halt()` in `install_reports.py` UPDATE-then-INSERT into `install_reports` so the halt is captured even if it beats `/report/start`. Columns `halted_at`, `halt_stage`, `halt_reason`, `halt_log_tail` added by migration 226. CSRF exempt via existing `/api/install/report/` prefix; auth via `X-Install-Token`. Closes the "uncertified-box-silently-stuck" class: the halt is now visible on `/admin/substrate-health` instead of masquerading as an `install_loop`.
- **v38 compat-match relaxed (Session 208).** `check_hardware_compat()` in `iso/appliance-image.nix:1012` now tries exact yq match first, then trim+lowercase normalized match against `.models | keys[]`, then substring-vs-known-tokens (`t740 t640 t630 t730`). Closes misses on `"HP t740 Thin Client "` (trailing space), `"HP T740 Thin Client"` (case drift), and future model-string cosmetic variance. `matched_key` passed forward so `.tested` lookup uses the winning yaml key. Halt path still writes the original `$HW_PRODUCT` into the support-ticket UI so the operator reports exactly what dmidecode saw.
- **Nix flake build needs `git add -A` on dirty worktrees.** `buildGoModule` + `src = ../appliance` filters sources via git index — if you rsync new files into a non-git-tracked copy of the repo (e.g. onto the VPS at `/root/Msp_Flakes`), Nix will exclude them. Symptom: `undefined: <symbol>` compile errors for code that exists on disk but not in `git ls-files`. Fix: `cd /root/Msp_Flakes && git add -A` before rebuilding. Do NOT try to work around it by shoe-horning `src = ./.` with a broader path filter; the git filter exists for a reason.
- **Pydantic dup baseline = 0 (Session 210-B 2026-04-25).** `test_no_new_duplicate_pydantic_model_names` EXPECTED_MAX = 0. Adding any new duplicate `__name__` across BaseModel subclasses fails CI. Class renames with unchanged field signature are auto-permitted by `pydantic_contract_check.py` (informational `RENAMED` line in pre-commit output) — wire-contract-safe since FastAPI's OpenAPI $ref key is the only thing that changes. `test_schema_is_deterministic` passes (no longer xfail). Determinism check is two-run: first run may write if Python version drifted (CI 3.11 vs dev 3.14), second run must say `unchanged`.
- **Migration 244 (Session 210-B):** `discovered_devices` UNIQUE(LOWER(mac), site_id) + 3 GC funcs (`prune_install_sessions(30d)`, `prune_nonces(4h)`, `prune_discovered_devices(60d)`). Wired into `data_hygiene_gc_loop` (24h cadence, 15-min startup delay). `discovered_devices_archive` preserves the dedup snapshot.
- **Migration 245 (Session 210-B): `relocations` table** is the append-only tracker for admin-initiated appliance moves. Columns: source_*, target_*, mac_address, status (pending/completed/expired/failed), reason, actor, fleet_order_id, evidence_bundle_id. UNIQUE(mac, status) blocks concurrent pending moves. `prevent_relocations_delete()` trigger blocks DELETE. `finalize_pending_relocations()` SQL func: target.last_checkin > initiated_at → 'completed' + soft-delete source + deactivate source api_keys; >30 min initiated_at → 'expired'. `relocation_finalize_loop` runs every 60s.
- **Migration 246 (Session 210-B):** `site_appliances.status` CHECK extended with `'relocating'` + `'relocated'`. Migration 245's relocate flow needs both; the check originally only allowed pending/online/offline/auth_failed/decommissioned.
- **Appliance relocate flow (Session 210-B RT-1..8).** `POST /api/sites/{site_id}/appliances/{appliance_id}/relocate` body `{target_site_id, reason}` (reason ≥20 chars). Same-org only (cross-org returns 403, points at future `/api/admin/cross-org-relocate`). Atomic txn in `admin_connection`: validate → UPSERT target site_appliances → mint api_key → mark source `'relocating'` (NOT eager soft-delete) → if `agent_version >= 0.4.11` issue signed `reprovision` fleet_order via `sign_admin_order(target_appliance_id=...)` else return ssh_snippet → write `relocations` row → emit `appliance_relocation` compliance_bundle (admin_initiated variant via `appliance_relocation.emit_admin_relocation_bundle`) → admin_audit_log. Frontend `MoveApplianceModal.tsx` renders fleet-order receipt OR copy-paste ssh_snippet. **Demoed end-to-end on 2026-04-25:** 89s elapsed initiate → completed (orphan moved physical-appliance-pilot-1aea78 → north-valley-branch-2 with no SSH).
- **Daemon `reprovision` order (v0.4.11+, Session 210-B).** `appliance/internal/orders/processor.go::handleReprovision`. Atomic config.yaml rewrite via tmpfile + rename, backup `/var/lib/msp/config.yaml.bak.reprovision.<unix>`, refuses api_keys < 40 chars, ACK-before-restart via 3s-delayed goroutine. Required params: `new_site_id`, `new_api_key`. Optional: `new_host_id`, `restart_delay_sec`. **TODO v0.4.12:** add `reprovision` to `dangerousOrderTypes` map (currently scoped to update_daemon, nixos_rebuild, healing, etc.) — task #164.
- **`relocation_stalled` substrate invariant (sev2, Session 210-B RT-4).** Fires when a `relocations` row stays `pending` for >30 min. Substrate runbook at `substrate_runbooks/relocation_stalled.md` walks the operator through the diagnostic ladder (daemon online? agent_version ≥ 0.4.11? fleet_order_id present? ssh_snippet was returned for legacy daemons?). Total invariant count after this addition: 33.
- **`fleet_orders` is FLEET-WIDE — no site_id/appliance_id columns.** Per-appliance scoping is via `target_appliance_id` embedded in the SIGNED payload (`processor.go::verifyHostScope` rejects mismatched). Use `order_signing.sign_admin_order(target_appliance_id=...)` for per-appliance orders (e.g. reprovision); `order_signing.sign_fleet_order` for true fleet-wide (e.g. update_daemon). The relocate endpoint's first version had a broken INSERT referencing nonexistent columns — fixed in `331b7d29`.
- **`admin_audit_log` column is `username`, not `actor`.** Schema: (id, user_id, username, action, target, details, ip_address, created_at). Two new endpoints (sites.py:relocate, provisioning.py:admin_restore) had `actor` and would have failed on first call — fixed in `24613c15`.
- **`/api/provision/admin/restore` (Session 210-B).** Admin Bearer-authed endpoint that UPSERTs site_appliances + mints fresh api_key + audit-logs in one txn. Built for the orphan class where the standard rekey endpoint refuses ('unknown appliance' because the row was deleted). Reason ≥20 chars enforced. Pairs with `recover_appliance_full.sh` script.
- **Class-level Pydantic rename detection in pre-commit (Session 210-B).** `scripts/pydantic_contract_check.py::_detect_class_renames` finds classes that disappeared from old AST + signature-match against added classes (sorted (name, type) tuples). Unambiguous matches log informational `RENAMED` line, don't block. Class rename + ANY field type change still surfaces as removals (signature differs). Two-class-same-signature is ambiguous — surfaces removals, no rename claim.
- **Pre-push lifespan-imports guard (Session 210-B).** `tests/test_lifespan_imports_resolve.py` walks main.py's AST, finds every `from dashboard_api.X import Y` inside `lifespan()`, asserts every Y is a top-level binding in X. Catches the deleted-symbol-deferred-import class that crashlooped prod earlier today (l2_auto_candidate_loop). AST-only — no asyncpg/pynacl/sqlalchemy required, runs cleanly in pre-push.
- **Substrate prod-snapshot fixture pattern (Session 210-B).** `tests/fixtures/substrate/<INV>/<scenario>.json` + `tests/test_substrate_prod_fixtures.py`. Captures real prod-shaped DB rows + expected violation count, replays through the assertion. Catches false positives at PR time. Add fixtures liberally — they're cheap, false-positive regressions are expensive.
- **DELETE-impact-check tool (Session 210-B).** `scripts/db_delete_safety_check.py --table site_appliances --where "appliance_id='X'"` runs an impact analysis against IMPACT_MAPS (api_keys, compliance_bundles, incidents, fleet_orders, install_sessions, discovered_devices). `--execute` requires `--reason` ≥20 chars + types-the-key confirmation + writes admin_audit_log. Mandatory before any DELETE on site_appliances/sites/api_keys.
- **`migrate.py` ON CONFLICT DO NOTHING (Session 210-B fix in 1eb7772c).** apply_migration's INSERT into schema_migrations didn't have ON CONFLICT, so migration 000 (which self-INSERTs its own row) collided. Latent forever in long-lived prod DBs (already had the row); blocked every fresh-DB initialization.
- **Sigauth interval bind (Session 210-B fix in 06a22794).** `signature_auth.py::_nonce_seen` MUST keep the `$2::interval` cast in the SQL even when binding a Python timedelta — Postgres needs the cast at PREPARE time before bind to resolve operator types. Without it: `UndefinedFunctionError: operator does not exist: timestamp with time zone > interval`. Removing the cast was wrong; the original f-string-as-text bind was also wrong. Right answer: timedelta bind + ::interval cast.
- **Session 210-B daemon v0.4.11 binary** at `https://api.osiriscare.net/updates/appliance-daemon-0.4.11`, sha256 `7a096b79786306ecdfdcb1b212a6be21cff94cb169d797955291a6e09cb1d302`. All 3 prod appliances rolled to 0.4.11 via `update_daemon` fleet order on 2026-04-25.
- **Daemon v0.4.12 (Session 210-B audit P1):** `reprovision` added to `dangerousOrderTypes`. Identity-rewriting orders MUST never execute pre-checkin without server-pubkey verification. Binary at `https://api.osiriscare.net/updates/appliance-daemon-0.4.12`, sha256 `5c59dff03ba218d87712b8397cedfda7ae828171c930503a2ad113dbc6620f95`. Locked in by `TestReprovisionInDangerousOrderTypes` Go test.
- **Daemon v0.4.13 (Session 211, #179):** Closes the sigauth identity-vs-evidence key design gap. Daemon now uploads `agent_identity_public_key` at every checkin (the IDENTITY pubkey it signs sigauth headers with — distinct from the evidence-bundle pubkey). Server persists in `site_appliances.agent_identity_public_key` (migration 251) and `signature_auth.py::_resolve_pubkey` reads it ahead of the legacy `v_current_appliance_identity` view. Binary at `https://api.osiriscare.net/updates/appliance-daemon-0.4.13`, sha256 `15e971565b3c1cb7f1b7b75c12d6ab864ff3c73bc6fcc5bea7758dbd3505c6ab`. Fleet rollout via fleet_order `ffc754a7-d4db-4296-9aa3-3dd795f46c9a` issued 2026-04-25 19:04Z. Substrate `signature_verification_failures` should drain on north-valley-branch-2 within ~1h of all 3 appliances rolling forward (rolling 1h sigauth_observations window).
- **`promoted_rules` natural unique key is (site_id, rule_id), NOT rule_id alone (Session 210-B Migration 247).** Same rule rolls out to many sites; each gets its own row. `INSERT ... ON CONFLICT (rule_id)` raises `InvalidColumnReferenceError` because rule_id has no unique constraint. Use `ON CONFLICT (site_id, rule_id) DO UPDATE`. The bug was in 3 places (flywheel_promote.py, client_portal.py, learning_api.py) — all fixed. Migration 247 added the UNIQUE INDEX. Tests: `test_sql_columns_match_schema.py` is the linter; static check for ON-CONFLICT-vs-unique-constraint is task #167.
- **Frontend mutation CSRF rule (Session 210-B audit P0).** Every state-changing fetch (POST/PUT/PATCH/DELETE) must EITHER use `fetchApi` (utils/api.ts auto-injects CSRF + credentials) OR include both `credentials: 'include'` AND `headers: { 'X-CSRF-Token': getCsrfTokenOrEmpty() }`. Raw fetches missing CSRF return 403 from the CSRF middleware. Test: `test_frontend_mutation_csrf.py` (ratchet baseline 58, drive to 0). Caught OperatorAckPanel + SensorStatus on first run.
- **Session 210-B end-to-end audit deliverables (`.agent/audits/`):** 5 reusable agent prompts for parallel "double flywheel class" scans — `duplicate_functions`, `sql_schema_columns`, `three_list_lockstep`, `bg_tasks_registry`, `endpoint_test_coverage` — plus a frontend-button audit shipped same-day. Cadence: post-major-refactor / pre-release / weekly during stable. Polarity rule: never auto-delete a duplicate without verifying which side is live (imported + registered in routes/task_defs).
- **`admin_transaction()` for multi-statement admin paths (Session 212).** `tenant_middleware.admin_transaction(pool)` is the canonical helper for any admin-context work that issues 2+ queries. Pins SET LOCAL app.is_admin + the queries to a single PgBouncer backend via an explicit transaction. Use for read-multi or read+write paths. **Single-statement reads stay on `admin_connection`** (the SET and the read share one pgbouncer transaction in that case). Routing-pathology fix shipped inline at sites.py:3565 (sigauth verify, commit 303421cc); centralized helper added in b62c91d2; symbolic adopters at prometheus_metrics.py:108 (f89802be) + device_sync.py:145 (92f2f73b). 230 more sites identified by audit — migrate as routing evidence surfaces.
- **`buildAuthedHeaders()` for partner mutations (Session 212).** `frontend/src/utils/csrf.ts::buildAuthedHeaders({apiKey, json?, extra?})` is the canonical headers-builder for partner fetches. Cookie + CSRF unconditional; X-API-Key additive. Replaces 7-file inline-templated shape that introduced the apiKey-precedence-ternary class on 2026-04-28 (commits 0c81fef6, efe413cf, 7c3e6551). All 7 partner files migrated in commit 39977ce4. CSRF gate test recognizes `buildAuthedHeaders(` as satisfying.
- **Migration 252 + 253 (Session 212).** 252 backfills aggregated_pattern_stats orphan rows from physical-appliance-pilot-1aea78 → north-valley-branch-2 (122 rows; merge-then-delete-then-rename pattern). 253 creates `promotion_audit_log_recovery` dead-letter queue + immutability trigger; the audit-log-savepoint failure path writes here on a FRESH admin_connection so the outer txn poison doesn't ride. Recovery via `scripts/recover_promotion_audit_log.py --apply` (idempotent, atomic INSERT-into-real-table + flag-flip in one transaction).
- **Session 212 substrate invariants (sev1/sev2):** `sigauth_enforce_mode_rejections` (sev2 — ANY enforce-mode fail in 6h; total invariant count 42→43); `promotion_audit_log_recovery_pending` (sev1 — HIPAA chain-of-custody gate, fires on any unrecovered DLQ row); `sigauth_post_fix_window_canary` (sev1 — window-bounded 2026-04-28 17:11Z → 2026-05-05 17:11Z; auto-disables via SQL date filter; **REMOVE from ALL_ASSERTIONS after 2026-05-05** if silent through window). Total count: 45.
- **Session 212 forensic logger.error contract (`signature_auth.py:323`):** `sigauth_unknown_pubkey` ERROR with extras `site_id`, `mac_address`, `ts_iso`, `nonce_hex` (8-hex truncated — correlation only, NOT a unique key), `sig_len`, `signature_enforcement_mode` (enforce/observe/unknown — the discriminator). Pinned by `tests/test_sigauth_forensic_logging.py` including a regression guard against re-adding the dropped `headers_present` key + a discriminator test that asserts mode='enforce' when lookup succeeds.
- **Session 212 CI gates added:** (a) `tests/test_no_silent_write_warnings.py` — regression-grep on `logger.warning` for DB-write phrases; bans the class. (b) `tests/test_test_filename_uniqueness.py` — Levenshtein < 3 between any two test_*.py stems fails CI; closes the test_flywheel_promote vs test_flywheel_promotion silent-skip class. (c) `.githooks/pre-push` runs `npx tsc --noEmit` when frontend/* changes (catches the 2026-04-28 90-min cascade class). Opt-in `PRE_PUSH_FULL=1` runs stem-matched backend pytests via filename-prefix discovery. (d) CSRF gate parser recognizes `buildAuthedHeaders(` as satisfying.
- **Session 212 `/api/version/drift` endpoint:** public, compares runtime_sha to GitHub `main` HEAD via `https://api.github.com/repos/jbouey/msp-flake/commits/main`. 60s cache + 5min stale-cache-eviction so a transient GitHub outage doesn't pin a stale SHA forever. GITHUB_TOKEN env lifts rate from 60/h to 5000/h. Operators can spot silent-cascade deploy failures within the cache window.
- **Session 212 task #169 closure (Option C HYBRID, user-override 2026-04-28 22:51Z):** wrap-fix at sites.py:3565 (commit 303421cc) accepted on 5h40m + 5 mandatory compensating controls — override section in validation doc, retained `scheduled_followups[0]`, sigauth_post_fix_window_canary substrate invariant, forensic-mode discriminator test, memory entry codifying override discipline. Reopen criterion: any fire of either sigauth invariant through 2026-05-05 17:11Z auto-reopens task and triggers pivot to MVCC / deleted_at / autovacuum hypothesis with new round-table BEFORE any fix. See `docs/security/sigauth-wrap-{rationale,validation}-2026-04-28.md`.
- **Migration 254 + 255 (Session 213).** 254 retries the orphan `aggregated_pattern_stats` cleanup as a full Step 1+2+3 (merge → delete-collisions → rename) — 252 left 115 orphan rows because asyncpg simple-query / explicit BEGIN/COMMIT skipped sub-statements after the first. 255 closes the **upstream recreation cycle**: every 30 min `_flywheel_promotion_loop` aggregates `execution_telemetry` by `site_id`, so cleanups land then orphan rows get RECREATED on the next tick. 255 relocates `execution_telemetry` (19,063 rows), `incidents` (533), `l2_decisions` (31) from `physical-appliance-pilot-1aea78` → `north-valley-branch-2`, then re-runs the `aggregated_pattern_stats` cleanup. **INTENTIONALLY skips `compliance_bundles`** (137,168 rows — Ed25519 + OTS-anchored; `site_id` is part of the cryptographic binding; auditor kits reconcile via the appliance relocation event).
- **`PhantomSiteRolloutError` precondition (Session 213 F2 P0).** `flywheel_promote.safe_rollout_promoted_rule` now refuses `scope='site'` when `SELECT 1 FROM site_appliances WHERE site_id=$1 AND deleted_at IS NULL LIMIT 1` returns 0 rows. Without it, every future relocate/decom was a latent **phantom-promotion**: the rollout would issue fleet_orders into a dead site and silently succeed. `routes.py::promote_pattern` translates to HTTP 409 with a structured remediation body pointing the operator at `admin_audit_log` for the relocate trail. `scope='fleet'` iterates live sites already so it doesn't need the gate.
- **`flywheel_orphan_telemetry` sev1 substrate invariant (Session 213 F3 P0).** Catches the upstream class that mig 255 closed manually: `execution_telemetry` rows landing under a `site_id` with no live appliances. Threshold: > 10 orphan rows per site_id in 24h. Wired into `ALL_ASSERTIONS` + `_DISPLAY_METADATA` + `substrate_runbooks/flywheel_orphan_telemetry.md` (three-list lockstep). Total invariant count: **45 → 46**. Runbook discriminates "known dead site (rename/decom — auto-relocate)" from "unknown site_id (escalate — possible spoofed appliance)."
- **Three layered flywheel bugs identified (Session 213 round-table verdict, NEEDS-IMPROVEMENT 7 findings):** (1) **Eligibility fragmentation** — evidence trapped under a dead site_id never reaches canonical promotion candidates [F1 shipped + mig 256]. (2) **Phantom void promotions** — rollouts to dead sites silently succeed [F2 shipped]. (3) **Recreation cycle** — cleanup futile without upstream fix [F3 shipped + mig 255]. P1 follow-ups: F4 centralize site rename behind SQL function + CI gate [shipped + mig 257]; F5 deprecate duplicate `_flywheel_promotion_loop` in `background_tasks.py` [**already closed in Session 210-B** — `test_flywheel_reconciliation_orphan_symmetry.py::test_l2_synthetic_ids_excluded_from_platform_aggregation` pins absence; the round-table flag was a duplicate-finding]. P3: F6 federation tier for eligibility thresholds; F7 operator diagnostic endpoint `GET /api/admin/sites/{site_id}/flywheel-diagnostic`.
- **`canonical_site_id()` STABLE function (Session 213 F1 P0, mig 256).** Resolves any `site_id` to its canonical via `site_canonical_mapping` chain (WITH RECURSIVE, depth-bounded 16, returns input on no-mapping). The mapping table is **append-only** — DELETE+UPDATE blocked by trigger (departure from mig 151/245 DELETE-only precedent because chain function correctness depends on immutable `to_site_id`). CHECK constraints: actor must match email regex (CLAUDE.md privileged-access rule, no 'system'/'migration:NNN'), reason ≥20 chars, no-self. Backfill row: `physical-appliance-pilot-1aea78 → north-valley-branch-2`. Use everywhere telemetry/operational queries aggregate by site_id; **NEVER for `compliance_bundles`** (Ed25519 + OTS bind to original site_id forever — CI gate `tests/test_canonical_not_used_for_compliance_bundles.py` enforces ±5-line proximity check). `_flywheel_promotion_loop` Step 1 + Step 3 in `main.py` use canonical_site_id() in CTE-wrapped SELECTs (single function call per row; planner-cache opportunity, not guarantee).
- **`rename_site()` centralized function (Session 213 F4 P1, mig 257).** The ONLY sanctioned path for renaming a site_id. Alias-style: original `sites` row stays at from_site_id (PK update class is intractable across the FK graph); `site_canonical_mapping` carries the read-side aliasing. Auto-discovers tables with `site_id` via `information_schema`; SKIPS `_rename_site_immutable_tables()` IMMUTABLE list (`sites`, all cryptographic tables compliance_bundles/evidence/ots_proofs/baa_signatures/audit_packages/compliance_attestations/compliance_scores/compliance_packets, all audit-class tables admin/client/partner audit_logs + portal_access_log + incident_remediation_steps + fleet_order_completions + sigauth_observations + promoted_rule_events + reconcile_events + appliance_audit_trail + journal_upload_events + promotion_audit_log, plus self-referential site_canonical_mapping + relocations). Validation: actor=email regex + reason ≥20chars + from≠to + from_site_id exists in `sites`. Guardrails: `statement_timeout=5min` + `pg_advisory_xact_lock(hashtext('rename_site:'||from))`. Bypasses mig 192 row-guard via `SET LOCAL app.allow_multi_row='true'`. FAIL-FAST — any UPDATE error rolls back canonical_mapping write too. Returns SETOF (touched_table TEXT, rows_affected BIGINT). Audit-logs `site.rename` to `admin_audit_log`.
- **CI gate `tests/test_no_direct_site_id_update.py` (Session 213 F4 P1).** Per-line `# noqa: rename-site-gate — <reason>` markers (Python/SQL/TS/Go); ratchet `NOQA_BASELINE_MAX=6` prevents silent accumulation. Whole-file exemption was rejected by round-table (P0 false-negative — silently allowed new violations in 5000-line files). Existing markers: 6 per-appliance MAC-scoped UPDATE site_id sites in `routes.py` + `sites.py` (claim, transfer, claim-v2 endpoints — genuinely per-appliance, not site renames). New direct `UPDATE … SET site_id =` outside an exempted line/file fails CI.
- **Migration 258 — canonical-aliasing read views (Session 213 F1-followup).** `v_canonical_telemetry`, `v_canonical_incidents`, `v_canonical_l2_decisions`, `v_canonical_aggregated_pattern_stats` — each projects `t.*, canonical_site_id(t.site_id) AS canonical_site_id`. **READ-ONLY enforced at three layers**: `INSTEAD NOTHING` rules on INSERT/UPDATE/DELETE + `security_barrier=true` + `REVOKE INSERT,UPDATE,DELETE FROM PUBLIC`. Round-table P0-DBA-1 caught that postgres makes these views auto-updatable by default. Per-row STABLE function cost warning in COMMENT ON VIEW: ad-hoc/aggregation use only, never hot paths.
- **Auditor-kit `chain.json["site_canonical_aliases"]` (Session 213 F1-followup P0-COMPLIANCE-1).** Auditor downloading the kit may see operational telemetry under a different site_id than the bundles. The aliases array lists every from→to mapping the substrate has recorded for this site_id (either direction) with actor/reason/related_migration/effective_at/direction. Closes the gap where mig 256 backfill mappings (no compliance_bundle counterpart) couldn't be reconciled in-kit. Compliance bundles still bind to issuing site_id forever.
- **`rename_site_immutable_list_drift` sev2 substrate invariant (Session 213 F4-followup, mig 258).** Auto-detects tables with `site_id` column AND DELETE-blocking trigger AND NOT in `_rename_site_immutable_tables()`. Catches future drift when new audit-class tables land without being added to the immutable list. Skips partition children via `NOT EXISTS pg_inherits` (mirrors `rename_site()` boundary). **Blind spot**: triggers attached to partition children only (not parent) — `appliance_heartbeats` case where mig 121 attached `prevent_heartbeat_deletion` to children only; the parent never appears in `delete_blocked` CTE. Doc-only — mig 259 still adds the parent to the list correctly. Total invariant count: **46 → 47**.
- **Migration 259 — drift close (Session 213 F4-followup).** Sev2 invariant fired on first deploy, surfaced 7 tables with site_id + DELETE-blocking trigger that weren't in the immutable list. Per-table inspection of trigger function source confirmed all 7 are intentionally append-only. Added: `appliance_heartbeats` (mig 121 partition ledger), `consent_request_tokens` (mig 189 magic-link consent chain), `integration_audit_log` (mig 015 integration audit), `liveness_claims` (mig 197/206 reconcile claim ledger), `promotion_audit_log_recovery` (mig 253 flywheel audit DLQ), `provisioning_claim_events` (mig 210 identity chain), `watchdog_events` (mig 217 watchdog attestation). Round-table verdict was first-pass READY (no rework). Notes: `consent_request_tokens` 72h TTL but lineage persists; `integration_audit_log.site_id` is UUID-typed so rename_site() would have errored anyway (belt-and-suspenders).
- **F7 operator endpoint `GET /api/admin/sites/{site_id}/flywheel-diagnostic` (Session 213 F7 P3).** Read-only aggregation of 7 sections for one site's flywheel state: canonical_aliasing (mig 256 mapping graph in/out), operational_health (live/relocating appliance counts, is_orphan), telemetry_recency (24h counts + orphan_telemetry_24h), flywheel_state (candidates + promoted_rules by lifecycle), pending_fleet_orders (sync_promoted_rule + update_daemon, #1 operator-incident need per round-table), substrate_signals (open violations, severity-sorted), recent_admin_events (last 5 audit-log touches). Plus `List[Recommendation]` (code/severity/message/action_hint — structured for frontend, not strings) and `notes` dict explaining input/canonical keying asymmetry. **P0 round-table catch**: must use `admin_transaction(pool)` not `admin_connection(pool)` — 11-query handler under PgBouncer transaction-pool would route SET LOCAL and fetches to different backends, RLS would hide every row → diagnostic returns silent zeros at the moment operator needs ground truth. **EXACT same class as Session 212 sigauth bug `303421cc`**. Rate-limited 20/min per actor. PHANTOM_PROMOTION_RISK uses `aged_promotion_candidates` (>1h) gate to suppress async cross-org false positives. ORPHAN_BUT_CANONICAL_TARGET reassuring rec when site is canonical FOR something. Auth: `require_admin`. Resolves input through `canonical_site_id()` so operator can pass orphan or canonical and get equivalent diagnostic.
- **Migration 260 — composite index `idx_execution_telemetry_site_created (site_id, created_at DESC)` (Session 213 F7-followup).** PRIMARILY benefits flywheel-diagnostic 3 queries with `site_id=$1 + created_at range` (exact strong shape). `flywheel_orphan_telemetry` invariant has `created_at` as leading predicate so benefits indirectly via GROUP BY post-filter only. Plain CREATE INDEX (not CONCURRENTLY) at 41 MB / 29K rows — sub-second build. Round-table flagged that CONCURRENTLY would require migrate.py framework support (can't run inside transaction); future hardening when a bigger table needs an index.
- **Auto-EXEMPT process bot for canonical-vs-compliance_bundles CI gate (Session 213 F4 closeout).** `_is_auto_exempt(path, source)` in `tests/test_canonical_not_used_for_compliance_bundles.py` returns True for migrations under `migrations/*.sql` that reference `_rename_site_immutable_tables`. Such migrations inherently document the immutable-list rationale (which mentions compliance_bundles), so the proximity check is a documentation co-mention, not a misuse. Closes the deploy-friction class that bit Session 213 twice (mig 257 + mig 259 both initially failed for legitimate documentation co-mention). Self-test pins discriminator behavior: positive (mig 257/259), negative (Python file + mig 254 without the function reference). Defense-in-depth inner scan (Session 214 P2 close): even within auto-EXEMPT files, `_inner_scan_misuse()` runs 3 regex patterns (UPDATE compliance_bundles + canonical_site_id within 500 chars; INSERT same; reverse-direction) with `_strip_sql_comments()` first so docs co-mention doesn't false-positive. 5 self-tests cover positive/negative paths.
- **Migration 260 — composite index `idx_execution_telemetry_site_created (site_id, created_at DESC)` (Session 213 F7-followup).** PRIMARILY benefits flywheel-diagnostic 3 queries with `site_id=$1 + created_at range` (exact strong shape — verified `Index Only Scan` in prod EXPLAIN). `flywheel_orphan_telemetry` invariant has `created_at` as leading predicate so benefits indirectly via GROUP BY post-filter only. Plain CREATE INDEX (not CONCURRENTLY) at 41 MB / 29K rows — sub-second build.
- **Partition-aware drift detection (Session 214 P3 close).** `_check_rename_site_immutable_list_drift` in assertions.py now uses two-pass UNION query: Pass 1 = trigger directly on the table (relkind expansion `'r'` → `IN ('r', 'p')` is the actual fix — round-table corrected my original "trigger on children" narrative; mig 191 attaches `prevent_heartbeat_deletion` to the partitioned PARENT, not children). Pass 2 = trigger on a partition child → surface the PARENT (legacy/manual case). Empirically verified: `appliance_heartbeats` shows `relkind='p'` with `tgname='trg_prevent_heartbeat_deletion'` directly on the parent — Pass 1 catches it.
- **F6 federation tier — MVP slice (Session 214, mig 261).** `flywheel_eligibility_tiers` table (3 seed rows: local/org/platform), ALL `enabled=FALSE` + `calibrated_at=NULL` on ship. Schema-level CHECK constraints enforce calibration discipline: `org_isolation_required=TRUE` for tier_level >=1 (HIPAA boundary), `min_distinct_orgs/sites IS NOT NULL` required when `calibrated_at` is set. main.py Step 2 reads `FLYWHEEL_FEDERATION_ENABLED` env var with **lenient parser** (`in ("true","1","yes","on")` — matches `assertions.py::L2_ENABLED` sibling, NOT the strict `== "true"` used by stripe/evidence/substrate paths). **Triple-gate activation:** env=true AND tier.enabled=TRUE AND calibrated_at IS NOT NULL. Default OFF. Tier 1 + Tier 2 read paths + threshold calibration deferred to F6 phase 2/3 (multi-day, cross-org HIPAA round-table required). Spec: `docs/specs/2026-04-30-f6-federation-eligibility-tier-design.md`.
- **`flywheel_federation_misconfigured` sev3 substrate invariant (Session 214 F6 fast-follow).** Fires when `FLYWHEEL_FEDERATION_ENABLED` env var is truthy but no tier has `enabled=TRUE AND calibrated_at IS NOT NULL`. Read path falls back to hardcoded defaults defensively + emits `logger.warning` per loop tick — sev3 because no behavior degradation, operator-visibility only. Three-list lockstep: Assertion + `_DISPLAY_METADATA` + `substrate_runbooks/flywheel_federation_misconfigured.md`. Lenient env parser matches main.py + sibling `L2_pipeline_silent`. Total invariant count: **47 → 48** (+1 sev1, +1 sev2, +1 sev3 net of session 213+214).
- **F6 phase 2 Option A — read-only eligibility helpers (Session 214).** `flywheel_eligibility_queries.py` ships pure functions: `count_tier_local_eligible`, `count_tier_org_eligible`, `count_tier_platform_eligible`, `compute_tier_resolution`. NO writes, NO enforcement, NO rollout. Cross-org boundary in `count_tier_org_eligible` SQL is empirically tested via `test_cross_org_isolation_property` (DB-gated): inserts fixture rows under 2 distinct client_org_ids, calls scoped to ONE org, asserts count + aggregated total stay scoped. Per-site freshness gate INSIDE the CTE before aggregation (prior `MAX(last_seen)` was OR-style across sites — operator could calibrate based on misleading counts). `site_scope` filters `status != 'inactive'` (sites uses status enum, not deleted_at — verified live schema). F7 endpoint extended with `tier_resolution` section + `TierThresholdsView` Pydantic model exposing actual threshold values per tier (operator transparency). Two type-drift guard tests assert all `TierThresholds` fields propagate to the projection helper + Pydantic model — same class as three-list-lockstep failures we've been bitten by.
- **`shared.parse_bool_env(var_name, default)` helper (Session 214).** Lenient env-flag parser (`true`/`1`/`yes`/`on`) — single source of truth across F7 diagnostic + future F6 enforcement. Round-table caught that strict `== "true"` would silently drop `FLYWHEEL_FEDERATION_ENABLED=1` and create a credibility event when F7 reports `tier_*_active` values disagreeing with what enforcement actually does. Matches sibling `assertions.py::L2_ENABLED` precedent.
- **F6 phase 2 enforcement DEFERRED (Session 214 round-table SHIP_FOUNDATION_SLICE consensus).** Tier 1 + Tier 2 eligibility-update WRITE path + `safe_rollout_promoted_rule` scope='org'/'platform' explicitly NOT shipped. Round-table reasoning: Tier 2 violates non-operator posture (Osiris making cross-org generalization decisions and auto-deploying via fleet_orders is operator-class, not substrate-class — even with PHI scrubbed, the *decision* crosses BAA boundary). Requires HIPAA §164.528 disclosure-accounting review by outside counsel + per-org consent attestation chain mirroring Privileged-Access Chain pattern. Tracking artifact at `.agent/plans/f6-phase-2-enforcement-deferred.md` with 5 explicit pre-conditions + cron schedule (7-day health check 2026-05-07, 21-day call-to-action 2026-05-21).
- **Migration 262 — F6 phase 2 foundation slice (Session 214).** `flywheel_federation_candidate_daily` table — daily snapshot of "patterns that WOULD clear current seed thresholds at tier N if federation were enforced." Surrogate BIGSERIAL PK + COALESCE-based UNIQUE index on (snapshot_date, tier_name, client_org_id) so platform rows (NULL client_org_id) work with ON CONFLICT. 4 CHECK constraints (tier_name valid set, count non-negative, success_rate range, **org_scope_matches_tier — tier='platform' MUST have NULL client_org_id; tier='org'/'local' MUST have non-NULL** — schema-level enforcement of design intent). Plus `promoted_rule_events.tier_at_promotion TEXT` (nullable, no CHECK yet — calibration round-table may adjust the value set before locking).
- **F6 federation-candidates admin endpoint (Session 214, mig 262 companion).** `GET /api/admin/flywheel/federation-candidates?tier=org|platform` — admin-only (require_admin), rate-limited 30/min/actor. Returns counts + summary; **NO rule_id-level disclosure across orgs** at the API layer (round-table-flagged constraint — counts are operator visibility into "what would be eligible," not the eligibility list itself). Pulls from snapshot table when present; falls back to live recompute via `count_tier_org_eligible` / `count_tier_platform_eligible` until first daily run. Daily snapshot loop `_flywheel_federation_snapshot_loop` in main.py (30-min startup delay, asyncio.sleep(86400) cadence). Snapshot writer `take_federation_snapshot` is property-tested to NOT touch `aggregated_pattern_stats` / `promoted_rules` / `fleet_orders` / `promoted_rule_events` — only writes to `flywheel_federation_candidate_daily`.
- **`cross_org_federation_leak` invariant — round-table BLOCKED (Session 214).** Attempted to ship a sev1 detector for cross-org `sync_promoted_rule` fleet_orders. Round-table identified a P0 Cartesian-product JOIN bug: `pr.rule_id = fo.parameters->>'rule_id'` matches multiple origin rows because `(site_id, rule_id)` is the natural unique key on `promoted_rules` (mig 247), not rule_id alone. Today this is masked because all sites are in one org; the moment a second tenant onboards, the detector fires false-positive sev1 ~100% of multi-tenant cross-org orders — wolf-crying-wolf failure mode for a privacy detector. Reverted local working-tree before push (never shipped to prod, invariant count stayed at 48). Captured 7 design pre-conditions in deferred card for the correct future invariant: UUID-based JOIN (`safe_rollout_promoted_rule` stamps `parameters->>'promoted_rule_id'` UUID PK; assertion joins on `pr.id::text`), prod-snapshot fixture for two-orgs-shared-rule_id, manual-resolve-only flag (auto-resolve is wrong default for sev0-class privacy trip-wire), SQL-fragment removal from remediation, `fleet_orders.notes` column verification, LIMIT bump + sibling defense-in-depth invariants.
- **Session 214 operative-holes audit closure (2026-05-01, 4 P0s + 5 P1s closed).** 4-fork adversarial audit + 5-block sequenced ship + per-block round-table gate. Final invariant count: **54** (+4 net: 50→54). New: `substrate_assertions_meta_silent` (sev1 — Block 2 P0; the watcher needs a watcher), `bg_loop_silent` (sev2 — Block 2 P0; closes the silent-stuck-await class for the ~50 supervised loops; `_supervised` registers a startup heartbeat so every loop appears in the registry), `compliance_packets_stalled` (sev1 — Block 4 P1; HIPAA §164.316(b)(2)(i) 6-yr retention class), `partition_maintainer_dry` (sev1 — Block 4 P1; covers compliance_bundles + portal_access_log + appliance_heartbeats + promoted_rule_events). `bg_heartbeat.EXPECTED_INTERVAL_S` calibration gotcha: `health_monitor` was 60 should be 300 — caught by the very invariant on first deploy (false-positive auto-resolved post-fix `e820ca85`). Rule: when adding a loop to EXPECTED_INTERVAL_S, the value MUST match the actual `asyncio.sleep(N)` cadence in the loop body — drift = false positives. Final commit chain: `8d43071e` → `ffb50996` → `7e71fc35` → `0aa2d519` → `721c06ba` → `b40ed71f` → `3ddf1475` → `e820ca85`.
- **`admin_transaction(pool)` adoption pass (Session 214 Block 1, 2026-05-01).** 4 sites swapped from `admin_connection` / raw `pool.acquire()` → `admin_transaction(pool)` (multi-statement admin paths require the txn-pinned variant per the Session 212 routing-pathology fix). Sites: `_go_agent_status_decay_loop` (main.py), `relocate_appliance` (sites.py:1983), `admin_restore_appliance` (provisioning.py:876), `mark_stale_appliances_loop` (background_tasks.py:1679). Regression test `tests/test_admin_transaction_for_multistatement.py` is the per-line CI gate — pinned by AST-search. **Audit trail in `go_agent_status_events` was MVCC-race-vulnerable in the c270bb76 → 8d43071e window**; pre-fix flap-rows are factually-tagged but operator-only (not Ed25519, not in auditor kit) — disclosure entry at `memory/feedback_session_214_block_1_mvcc_disclosure.md`. Post-fix uses 3-CTE single-statement with `FOR UPDATE` (NOT `SKIP LOCKED` — Brian P0 catch: SKIP LOCKED would silently miss heartbeat-upserted rows, far worse than the race it would close).
- **Per-device savepoint pattern in `/api/devices/sync` (Session 214 Block 3, 2026-05-01).** Active prod 500 root cause: the per-device for-loop in `device_sync.py::sync_devices` had a bare `try/except: errors.append` swallowing PG errors mid-iteration. Per CLAUDE.md asyncpg savepoint invariant, Python's try/except does NOT reset PG txn state. One device's FK violation poisoned the entire request. Fix: wrap the 302-line per-device body in `async with conn.transaction():` (nested savepoint). Failure rolls back THAT device only; outer admin_transaction continues. Verified post-deploy: 500 rate dropped from 26+/30min to 0/5min on commit `b40ed71f`. Pattern generalizes: any for-loop body with bare `except: pass` or `except: errors.append` on conn.execute/fetch* MUST have a savepoint.
- **`($N || ' unit')::INTERVAL` lint repo-wide (Session 214 Block 3).** The Apr-30 P0 hot-fix (commit 903746bd) shipped a 2-file lint; Block 3 expanded scope to glob ALL backend Python (excl tests/, fixtures/, test_*.py prefix). Tightened regex to `\(\s*(?:\$\d+|:\w+)\s*\|\|\s*'[^']+?'\s*\)\s*::\s*INTERVAL` — column-driven concat (`consent_ttl_days || ' days'`) is asyncpg-safe and intentionally NOT caught. Fixed 7 production sites (partners.py, partner_invites_api.py, l2_planner.py, routes.py × 4) — all dropped `str(N)` casting and use `make_interval(<unit> => $N)`. CI gate at `tests/test_flywheel_eligibility_queries.py::test_no_string_concat_interval_pattern`. **Companion fix to schema-column lint** (`tests/test_sql_columns_match_schema.py`): UPDATE walker regex now uses `(?!>)` negative lookahead to exclude `name =>` named-arg syntax — was false-positiving every `make_interval(hours => $1)` use.
- **`logger.warning` → `logger.error` on DB-write paths (Session 214 Block 3).** CLAUDE.md "no silent write failures" rule was unevenly enforced. Fixed 4 sites: `provisioning.py:611,645`, `main.py:1759,1971` (compliance_packet auto-gen — silent HIPAA gap class). All upgraded to `logger.error(exc_info=True, extra={...})` so the log shipper alerts. The compliance_packets one is paired with the new `compliance_packets_stalled` outcome-layer invariant for defense-in-depth.
- **Session 214 P0.2 deploy regression + recovery (2026-05-01).** mig 264 added `'auto_recovered'` (14 chars) to the `incidents.resolution_tier` CHECK list — but the column is VARCHAR(10). Postgres accepts oversized CHECK literals at constraint creation; the failure surfaces only at INSERT runtime as `StringDataRightTruncationError`. Result: every `/api/evidence/sites/.../submit` 500'd for 35 min until substrate's own `evidence_chain_stalled` (Block 4 sev1) + `bg_loop_silent` (Block 2 sev2) caught the meta-failure and the round-table dispatched a hot-fix. Two ALTER COLUMN TYPE attempts (mig 266 v1 + v2) blocked by `v_canonical_incidents` view AND `partner_site_weekly_rollup` materialized view — drop+recreate of the matview requires REFRESH that takes minutes on prod-sized data. Round-table 5/5 chose the **shorten-value path** (mig 267): 'auto_recovered' → 'recovered' (9 chars, fits VARCHAR(10)). Final commit chain: `eb1f4142` → `a413dc81` (failed) → `38960008` (failed) → `bd79b62a` (recovered). Lesson: CI gate at `tests/test_check_constraint_fits_column.py` now parses every migration's `ADD CONSTRAINT … CHECK … IN (literals…)` and asserts max literal length ≤ column width (D6 closure). Forensic forensics chain unaffected (auto_recovered never persisted to prod).
- **Session 214 P3 score=0 fleet-wide closure (2026-05-01, mig 268).** Pre-fix: `compliance_scores.score_percentage = 0.00` on every appliance fleet-wide for many weeks. Root cause: writer/reader column mismatch — `evidence_chain.py::submit_evidence` populated `compliance_bundles.check_result` (and never `appliance_id` / `outcome`); but `calculate_compliance_score` joined on `cb.appliance_id` and filtered on `cb.outcome`. All 245,142 bundles had NULL on both columns. Round-table 5/5 chose **rewrite the function** (Path B) over **backfill 245K rows** (Path A — would visit every monthly partition during just-recovered prod). Mig 268: function joins via `site_appliances` natural-key chain (mig-265 sibling), uses `cb.check_result` IN-list `{pass, compliant, warning}`, switches `($N || ' days')::INTERVAL` → `make_interval(days => N)` (closes the lint gap inside SQL function bodies). `compliance_bundles.appliance_id` and `compliance_bundles.outcome` marked DEPRECATED via `COMMENT ON COLUMN`. Post-deploy: scores jumped from 0% to 60% on north-valley (10 controls, 6 passing, 4 failing). Forensic disclosure: pre-fix 0.00 was meaningless data (writer/reader mismatch), not a real failing-check signal — below public-advisory threshold. Memory entry at `memory/feedback_score_zero_disclosure_2026_05_01.md`. Final commit: `fad93ac6`.
- **`recovered` resolution tier semantics (Session 214, mig 267).** Distinguishable values on `incidents.resolution_tier`: `L1`/`L2`/`L3` (healing pipeline closed via remediation); `monitoring` (7d stale sweep gave up); `recovered` (fail→pass transition on bundle ingest — P0.2 hook in `evidence_chain.py::submit_evidence`); NULL (pre-tier-tracking). The shorter `recovered` (9 chars) replaced the original `auto_recovered` (14 chars) after the VARCHAR(10) column-width outage forced the round-table to choose between extending the column (blocked by view dependencies) and shortening the value. Forensic queries: `WHERE resolution_tier = 'recovered'` for the auto-recovery class. Followup card at `scheduled_followups` 2026-05-08 to extend column width cleanly (3-commit migration drops+recreates the dependent view + matview).
- **Score granularity gap (Session 214 D1 deferred 2026-05-01).** `calculate_compliance_score` (mig 268) joins to `evidence_framework_mappings` per-control but assigns the BUNDLE-level `cb.check_result` to each control via DISTINCT ON. So a bundle covering 5 controls with 1 actual failing host scores ALL 5 controls as failing. Pre-existing semantics, exposed by mig 268. Not a regression — function was previously returning 0 for everything. Fixing requires schema change (`evidence_framework_mappings.check_status` column + writer + mapper update) — multi-hour design work, NOT same-session. Tracked as P2 calm-session followup. Customer impact: scores under-report compliance (CONSERVATIVE) — better than over-reporting from a HIPAA posture.
