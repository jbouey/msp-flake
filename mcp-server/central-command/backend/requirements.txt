# CI test dependencies — MUST match requirements.lock (the Docker image pin)
# or CI silently tests a different FastAPI/Pydantic than what prod runs.
#
# Session 210 (2026-04-24) post-mortem: 3-hour prod outage because this
# file had fastapi==0.135.3 while requirements.lock had 0.115.6. The
# 0.135.3 relaxed the status_code=204 + return-annotation assert; 0.115.6
# enforces it. CI smoke-import passed, container crashlooped on import.
# Any drift between this file and requirements.lock is a silent
# CI-vs-prod test falsification — treat as a P0 correction.
fastapi==0.115.6
pydantic==2.10.3
sqlalchemy==2.0.36
asyncpg==0.30.0
httpx==0.28.1
structlog==24.4.0
cryptography==44.0.0
aiohttp==3.13.3
pynacl==1.5.0
python-multipart==0.0.20
email-validator==2.3.0
PyYAML==6.0.2
jinja2==3.1.4
redis==5.2.1
bcrypt==4.2.1
pyotp==2.9.0
minio==7.2.13
uvicorn==0.32.1
