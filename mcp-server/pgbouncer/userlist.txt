;; PgBouncer user authentication file
;; Format: "username" "password"
;; PgBouncer hashes passwords for SCRAM-SHA-256 exchange.
;; Password must match PostgreSQL role password.

"mcp_app" "REPLACE_WITH_ACTUAL_PASSWORD"
"mcp" "REPLACE_WITH_ACTUAL_PASSWORD"
