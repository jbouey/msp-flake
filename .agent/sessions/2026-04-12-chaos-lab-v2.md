# Chaos Lab v2 — Session 205

**Date:** 2026-04-12  
**Host:** MaCs-iMac.local (192.168.88.50)  
**Session:** 205 continuation

---

## Problem Diagnosed

Fleet was showing 32 `rogue_scheduled_tasks` incidents/day, 32 `defender_exclusions`, 28
`windows_update` — all on ws01 (192.168.88.251). User suspected continuous attacks.

**Actual root cause:** Chaos lab cron hadn't attacked since April 1 (11 days prior).
The incidents were from **infected VM state** left behind by old chaos runs.
ws01's Windows Task Scheduler had 9 persistent rogue tasks baked in:
- ChaosBackdoor
- MaliciousTask
- SystemHealthCheck
- SystemMaintenance
- SystemMaintenanceTask
- SystemOptimizer
- SystemStartup
- SystemStartupPersistence
- SystemUpdate

Each task had a daily schedule. On firing, it'd modify defender exclusions /
registry / service state, the appliance would detect + heal the symptom, but
the task XML remained to fire again next cycle.

---

## Actions Taken on iMac

### 1. Cleaned ws01 (192.168.88.251)
WinRM PowerShell via `chaos-lab/scripts/winrm_attack.py`:
- `Unregister-ScheduledTask` on all 9 rogue tasks
- Deleted XML templates from `C:\Windows\System32\Tasks\`
- Removed non-system defender exclusions
- Verified: **0 remaining rogue tasks**

Next appliance scan: **19 Windows checks, 0 failing** on ws01.

### 2. Clean snapshots of all 4 VMs
DC previously had NO snapshots (that's why restores broke AD clock — nothing
to restore to). Now all 4 have a known-good baseline named `clean-2026-04-12`:
- `northvalley-dc` → UUID 1634a5f7-6264-4eda-99ed-484d52166f0f
- `northvalley-ws01` → UUID f5263585-bc4a-4cfc-95cd-8b0b3d0d1490
- `northvalley-srv01` → UUID eaaf939e-2dc9-4de2-aec1-2c058d140225
- `northvalley-linux` → UUID d3d4d039-bdd8-46d0-9c0f-5bebc960d519

### 3. Three MITRE-aligned attack scenarios
Under `~jrelly/chaos-lab/v2-scenarios/`:
- `SCN_T1053_scheduled_task.ps1` — T1053.005 Scheduled Task persistence
- `SCN_T1562_defender_exclusion.ps1` — T1562.001 Impair Defenses
- `SCN_T1547_registry_run.ps1` — T1547.001 Registry Run Key persistence

Each is single-purpose and realistic (legitimate-looking name patterns).

### 4. Orchestrator `~jrelly/chaos-lab/v2-orchestrator.sh`
Three commands:
- `cadence <scenario>` — inject + observe 30 min + record pre/post score
- `nightly-restore` — sequential snapshot revert if daily score <70%
- `daily-score` — end-of-day score snapshot

Writes to `~jrelly/chaos-lab/v2-logs/`:
- `v2-YYYY-MM-DD.log` — per-day orchestrator log
- `cadence-results.csv` — all cadence outcomes with pre/post scores
- `daily-scores.csv` — end-of-day score history

### 5. New crontab (replaced old lab-cadence-check entry)
- **09:00** — Inject SCN_T1053 (scheduled task)
- **13:00** — Inject SCN_T1562 (defender exclusion)
- **17:00** — Inject SCN_T1547 (registry run)
- **23:30** — Record daily score
- **02:00** — Conditional sequential restore (only if daily score <70)
- Email reports 6/18, log cleanup 0

### Nightly restore sequence
Addresses user's "snapshot restore messed up AD clock" concern:
1. Shutdown workstations first (ACPI power button, 2min grace)
2. Force poweroff any still running
3. Shutdown DC last (ACPI, 2min grace, then force)
4. Restore DC snapshot, start DC alone, **wait 5 min for AD + time sync**
5. Restore workstations sequentially (2 min apart)

Only fires if end-of-day compliance score <70%.

---

## Telemetry Gaps (To Fix)

- `v2-orchestrator.sh fetch_score()` hits `$API/api/public/status/<site_id>`
  which doesn't exist yet. Falls back to 100 when unreachable, so restore
  logic fails-safe (won't trigger unwanted restores). Needs a public
  read-only score endpoint or changes to use authenticated admin endpoint.
- No scenarios table yet — cadence results are CSV-only. Future: a
  `chaos_scenarios` table in the backend DB for proper dashboards.

---

## Next Iteration Ideas

- Source-of-truth reconciliation in the agent ("time travel" detection):
  when a host reboots with wrong uptime or unexpected state change, agent
  calls appliance to get baseline and re-applies.
- Pattern classification layer (DBA's proposal): classify incidents as
  transient / episodic / chronic_infection / liveness, with different
  handling per category.
- More MITRE ATT&CK scenarios: T1078 (valid accounts), T1059 (command
  interpreter), T1055 (process injection) — run one per week on rotation.
